### Turn Summary
Implemented Phase 8 fixture fixes and default enforcement per spec:59 (per-reflection SHALL be default). Fixed test fixture to inject crystal_symmetry and build 4D HKL indices grid from metadata bounds; changed RefinementConfig default from "shell" to "per_reflection"; fixed UnboundLocalError in per-reflection mode branching (config_stage_b_mode_override initialization); fixed TypeError in initialize_asu_modifiers call (n_asu → n_asu_unique parameter). Validation blocked by KeyError 'shell_indices' during Stage B closure execution. Per-reflection ASU mapping now succeeds (n_asu ~34K unique reflections, Friedel folding working correctly, no fallback warnings), proving Phase 6 helpers operational, but closure hits missing key in param_values dict. Root cause: incomplete mode isolation in _optimize_stage_b_lbfgs — closure expects both per_reflection and shell keys regardless of mode. Next: debug param_values dict assembly and closure's mode-aware key access patterns in the 700-line Stage B optimization function.

Artifacts: plans/active/TORCH-REFINE-004/reports/2025-11-24T095000Z/ (compilation_check.log PASS default=per_reflection, pytest_phase6_regression.log 5/5 PASS 1.04s, pytest_per_reflection_smoke_retry3.log FAIL KeyError 'shell_indices')
