  * 2025-12-22T150000Z (Phase C.18 complete — spot-profile energy partition instrumentation) — Extended `plans/active/ARCH-SIM-CONSTRUCTION-001/bin/compare_stage_a_baseline.py` to compute full-panel spot-profile metrics (ROI vs halo vs FWHM energy partitions) for each matched reflection. Added `--collect-spot-profiles` and `--spot-profile-halo-pixels` CLI flags (default=10, clamped ≥1). Implemented `compute_spot_profiles()` helper computing per-ROI: ROI energy (sum inside bbox), halo energy (sum in expanded bbox ± halo_pixels, clamped to detector bounds), ROI fraction of halo, ROI fraction of panel, peak value, and FWHM fast/slow (width of contiguous pixels ≥ 0.5 × peak). Added `_summarize_spot_profiles()` to generate Markdown report listing median fractions plus top-5 ROIs by off-ROI energy and narrowest FWHM. Ran spot-profile probe in baseline mode with halo=10 pixels. Results: 27 reflections matched, median ROI fraction of halo=0.6756 (67.56% of halo energy inside ROI), median ROI fraction of panel=4.2e-5 (spots occupy tiny panel fraction). Top-5 worst off-ROI spill: ROI fractions 0.0054–0.0181 (95–98% energy outside ROI), most with FWHM=0×0 (below threshold). Summary markdown shows HKL indices, bboxes, and energy metrics for reviewers. DB-AT-028/029 tests still FAIL with same signature: chi²/pixel=2.097e+05, median ROI corr=-0.053. Spot-profile evidence suggests ≥80% of Stage A energy sits outside DIALS ROI bboxes for worst reflections, consistent with transformation ledger hypothesis that nanobrag_torch concentrates intensity into narrower footprints than DIALS expects. However, median 0.6756 ROI fraction (majority of energy inside ROI) contradicts simple "all energy outside ROI" hypothesis. Next action: supervisor should analyze spot_profile_summary.md to determine if ≥80% halo spill threshold from input.md is met (requiring nanobrag_torch debug patch per Environment Freeze exception), or if narrower FWHM + ROI alignment issue explains the chi² failure. Artifacts: `plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-22T150000Z/` (spot_profile_probe.json, spot_profile_probe.log, spot_profile_summary.md with top-5 tables, db_at_028/pytest.log, db_at_029/pytest.log). Metrics: 1 file touched (compare_stage_a_baseline.py), +195 lines (spot-profile logic + markdown writer), 27 reflections profiled, median ROI/halo=0.6756, worst-5 ROI/halo range 0.0054–0.0181.
