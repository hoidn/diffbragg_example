Summary: Add a partiality ledger to the Stage A baseline probe so we can quantify `|F|²·F_latt²·LP` vs Stage A per ROI and pinpoint the simulator term responsible for the 50× intensity deficit before scheduling another physics change.
Mode: Parity
InitiativeType: architecture
Focus: ARCH-SIM-CONSTRUCTION-001 — Simulator Construction Convention Alignment
Mapped tests:
  - AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md DBEX_SMOKE_SIGMA_SOURCE=cli_override DBEX_SMOKE_DETECTOR_SIZE=small DBEX_SMOKE_CALIB_PATH=sp.proc/calibration/config_torch_smoke_small.json DBEX_SMOKE_HKL_PATH=sp.proc/calibration/smoke_refined_structure_factors_small.mtz KMP_DUPLICATE_LIB_OK=TRUE NANOBRAGG_DISABLE_COMPILE=1 python plans/active/ARCH-SIM-CONSTRUCTION-001/bin/compare_stage_a_baseline.py --geometry-mode baseline --stage-a-mosaic-domains 16 --collect-hkl-stats --collect-spot-profiles --collect-orientation-metrics --collect-physics-ledger --collect-partiality-ledger --output plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-25T010000Z/stage_a_baseline_probe_baseline.json \| tee plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-25T010000Z/stage_a_baseline_probe_baseline.log
  - AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md DBAT028_ARTIFACT_DIR=plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-25T010000Z/db_at_028 DBEX_SMOKE_SIGMA_SOURCE=cli_override DBEX_SMOKE_DETECTOR_SIZE=small KMP_DUPLICATE_LIB_OK=TRUE NANOBRAGG_DISABLE_COMPILE=1 pytest -vv tests/dbex/test_stage_a_smoke_parity.py::test_db_at_028_loss_scale_sanity \| tee plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-25T010000Z/db_at_028/pytest.log
  - AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md DBAT029_ARTIFACT_DIR=plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-25T010000Z/db_at_029 DBEX_SMOKE_SIGMA_SOURCE=cli_override DBEX_SMOKE_DETECTOR_SIZE=small KMP_DUPLICATE_LIB_OK=TRUE NANOBRAGG_DISABLE_COMPILE=1 pytest -vv tests/dbex/test_stage_a_smoke_parity.py::test_db_at_029_structure_parity \| tee plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-25T010000Z/db_at_029/pytest.log
Artifacts: plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-25T010000Z/

Do Now (hard validity contract):
- Implement: `plans/active/ARCH-SIM-CONSTRUCTION-001/bin/compare_stage_a_baseline.py::compute_partiality_ledger`
  - Add a `--collect-partiality-ledger` CLI flag (default off) that requires `--collect-orientation-metrics`. When enabled, reuse the existing reflection matches (with `h_frac`, Δhkl, 2θ, and `amp_sq_per_pixel`) plus calibration metadata (`mapping_context.calibration["crystal"]["N_cells"]`, crystal shape/fudge) to reproduce the `F_latt` factor from `nanobrag_torch.simulator.compute_physics_for_position`. Compute `F_latt` for SQUARE crystals via sincg(π·h,k,l) with the calibration N_cells, fall back gracefully when calibration is missing, and add guards so ROIs without valid metrics are skipped with an error note.
  - For every matched ROI with valid data, store `partiality_metrics = {"f_latt": ..., "partiality_factor": F_latt**2, "stagea_vs_f_latt_ratio": stage_a_mean / (amp_sq_per_pixel * partiality_factor), "stagea_vs_partial_lp_ratio": stage_a_mean / (amp_sq_per_pixel * partiality_factor * lp_factor)}`. If LP data was not already computed, derive Lorentz/polarization from the stored 2θ using the same formulas as `compute_physics_ledger`. Persist a new `partiality_alignment` block in the JSON output with global stats, resolution bins, and worst/best-5 tables, and append a Markdown section (mirroring the physics ledger) to `spot_profile_summary.md`.
  - Ensure the new ledger metadata (enabled flag, error strings, summary) is threaded into the probe metadata dict so downstream automation can cite it, and keep the baseline probe default behavior (flag off) unchanged.
- Validate the instrumentation by rerunning the mapped probe command (with the new flag) plus DB-AT-028/029 to prove the ledger captures the still-failing signature and to provide decision-carrying artifacts for the next simulator edit.

Deterministic Parity Crisis:
- Independent Reference: The DIALS reflection table `sp.proc/refGeom_small/refGeom_small.refl` feeding DB-AT-028/029 supplies independent `intensity.sum.value` / ROI masks (consumed via `compare_stage_a_baseline.py:1504-1631`) and stays ≈1.0× the target (`target_vs_ref=1.02` in `spot_profile_summary.md:81-122`), so it is not affected by Stage A simulators.
- Transformation Ledger:
  | Field | Units/frame | Producer (file:line) | Consumer (file:line) | Observed evidence | Hypothesis |
  | --- | --- | --- | --- | --- | --- |
  | ROI panel 0 [644:656,21:33] HKL (-7,5,-3) Stage A mean | ADU/pixel (Stage A warm cache) | dbex/refinement/stage_a.py:451-519 (caching + scaling bragg_zero_iter) | plans/active/ARCH-SIM-CONSTRUCTION-001/bin/compare_stage_a_baseline.py:1581-1709 (reflection ledger) | `Stage A/Ref = 3.71e-05` and `Stage A/|F|²·LP ≈ 0` with LP=2.61 despite target/ref ≈1 (spot_profile_summary.md:66-106); chi²/pixel initial = 2.1e5 (db_at_028/db_at_028_metrics.json:2-47). | Large positive LP boost should amplify this ROI, so the collapse points to missing lattice/partiality weighting downstream of |F|². |
  | ROI panel 0 [661:673,940:952] HKL (1,-9,4) Stage A mean | ADU/pixel | dbex/refinement/stage_a.py:451-519 | compare_stage_a_baseline.py:1581-1709 | `Stage A/Ref = 8.46e-02`, `Stage A/|F|²·LP ≈ 0` with LP=3.01 while |Δhkl|=0.233 (spot_profile_summary.md:66-105). | Geometry is within tolerance but Stage A intensity vanishes, implicating the per-reflection partiality kernel. |
  | ROI panel 0 [535:547,842:854] HKL (2,-6,2) Stage A mean | ADU/pixel | dbex/refinement/stage_a.py:451-519 | compare_stage_a_baseline.py:1581-1709 | `Stage A/Ref = 1.32e-02`, `Stage A/|F|²·LP ≈ 0` with LP=4.16 (spot_profile_summary.md:66-105). | Higher-resolution reflections accrue the largest LP factors but still collapse, pointing to the missing `F_latt` modulation. |
  | ROI panel 0 [257:269,37:49] HKL (-3,8,-9) Stage A mean | ADU/pixel | dbex/refinement/stage_a.py:451-519 | compare_stage_a_baseline.py:1581-1709 | `Stage A/Ref = 1.46e-03`, `Stage A/|F|²·LP = 1e-4` with LP=2.48 and ROI fraction of halo = 0.0112 (spot_profile_summary.md:24-106). | Energy spills outside ROI while LP gain goes missing, so quantifying `F_latt` per ROI is required before editing simulator code. |
  | ROI panel 0 [144:156,619:631] HKL (4,1,-6) Stage A mean | ADU/pixel | dbex/refinement/stage_a.py:451-519 | compare_stage_a_baseline.py:1581-1709 | `Stage A/Ref = 2.20e+00` yet `Stage A/|F|²·LP = 4.78e-02` (spot_profile_summary.md:110-116), so even the “best” ROIs retain a 20× deficit. | The residual spread after Lorentz indicates another systematic weighting term is missing; partiality ledger will tell if `F_latt` is the culprit. |
  | ROI panel 0 [739:751,610:622] HKL (-2,-4,4) Stage A mean | ADU/pixel | dbex/refinement/stage_a.py:451-519 | compare_stage_a_baseline.py:1581-1709 | `Stage A/Ref = 2.74e+00`, `Stage A/|F|²·LP = 5.37e-01` (spot_profile_summary.md:110-116) ⇒ best case is still ~0.5× expected. | Even the outliers that fare “well” trail the independent reference, underscoring that we need per-ROI `F_latt` evidence before touching simulator physics again. |
- Boundary Bisection Step: Once the new partiality ledger lands, compare Stage A vs `|F|²·F_latt²·LP` per ROI; if ratios stay ≫1 or ≪1, instrument nanobrag_torch to emit the actual per-reflection `F_latt` / polarization tensors before authoring another simulator fix.

How-To Map:
1. Run the updated baseline probe command (Mapped test #1) with `--collect-partiality-ledger` so `stage_a_baseline_probe_baseline.json` and `spot_profile_summary.md` capture the new ledger alongside existing HKL/orientation/physics evidence.
2. Execute `pytest -vv tests/dbex/test_stage_a_smoke_parity.py::test_db_at_028_loss_scale_sanity` with `DBAT028_ARTIFACT_DIR` pointing at the new report so chi²/ROI metrics cite the same simulator run.
3. Execute `pytest -vv tests/dbex/test_stage_a_smoke_parity.py::test_db_at_029_structure_parity` with matching env vars and artifact path.

Pitfalls:
- `--collect-partiality-ledger` must imply `--collect-orientation-metrics`; bail early with an actionable error when the prerequisite flag is absent.
- Calibration files without `crystal.N_cells` should log a warning and disable the ledger instead of emitting nonsense ratios; record that in probe metadata.
- Handle sinc singularities where `sin(π·h)` → 0 by using the analytic limit (1.0) so we do not introduce NaNs for integer HKLs.
- Keep the added math numpy/torch on CPU to avoid altering the actual Stage A run; the ledger is diagnostic-only.
- Do not mutate Stage A telemetry or reconstruction outputs while computing the ledger—read-only access to context/mapping objects only.
- Ensure the new JSON/Markdown sections live under the same report directory or automation will fail to locate them.
- Reuse existing reflection matching filters (e.g., skip ROIs without `amp_sq_per_pixel`) to avoid inflating ROI counts and violating probe-saturation policy.
- Keep the ledger opt-in so automated narrow-probe runs do not incur extra cost.
- Guard against stale `np.float64` vs `float` serialization in JSON (use `float()` when storing numbers) so downstream markdown/table generation doesn’t crash.

If Blocked:
- If calibration metadata lacks the N_cells tuple everywhere, log the blocker in `plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-25T010000Z/summary.md`, update `docs/fix_plan.md` + `galph_memory.md` with the missing-data reason, and request guidance before touching simulator code.
- If the new ledger crashes due to unexpected crystal shapes, disable the flag by default, capture the stack trace under the report directory, and mark the focus item blocked so we can consider a harness/spec change in the next loop.
