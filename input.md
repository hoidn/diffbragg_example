Summary: Prepare DB-AT-023 calibration guard implementation so `--adu-per-photon` wiring and bridge conversion land with acceptance tests and doc sync.
Mode: none
Focus: DB-AT-023 — Calibration policy guard
Branch: integration
Mapped tests: KMP_DUPLICATE_LIB_OK=TRUE pytest -v tests/dbex/test_calibration_policy.py -k DB_AT_023; pytest --collect-only tests -k DB_AT_023
Artifacts: plans/active/DB-AT-023/reports/2025-11-04T065500Z/{pytest_db_at_023.log,collect_db_at_023.log,calibration_metrics.json}
Do Now: [DB-AT-023] Implement: dbex/refine_one.py::create_parser (add `--adu-per-photon` plumbing) + dbex/nanobrag_bridge.py::prepare_refinement_inputs (apply photon conversion + metadata) + tests/dbex/test_calibration_policy.py::TestCalibrationPolicy (author DB_AT_023 acceptance tests); Validate: AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md KMP_DUPLICATE_LIB_OK=TRUE DBAT023_ARTIFACT_DIR=plans/active/DB-AT-023/reports/2025-11-04T065500Z pytest -v tests/dbex/test_calibration_policy.py -k DB_AT_023; Collect: pytest --collect-only tests -k DB_AT_023 | tee plans/active/DB-AT-023/reports/2025-11-04T065500Z/collect_db_at_023.log; Artifacts: plans/active/DB-AT-023/reports/2025-11-04T065500Z/
How-To Map: export AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md; export ARTIFACT_TS=2025-11-04T065500Z; mkdir -p plans/active/DB-AT-023/reports/${ARTIFACT_TS}; extend dbex/refine_one.py::create_parser to accept `--adu-per-photon` (float >0) and thread the value into `run_nanobrag_backend`; plumb the parsed value through DataLoad/bridge calls (e.g., pass to prepare_refinement_inputs); update dbex/nanobrag_bridge.py::RefinementInputs to carry `target_representation` and `global_scale_hint`, applying `(data - background)/adu_per_photon` when a positive factor is provided and leaving ADU otherwise; raise ValueError with actionable messaging if factor <=0 or missing when conversion requested; implement tests/dbex/test_calibration_policy.py with DB_AT_023 cases covering photon conversion correctness (using canonical fixtures when available), ADU path metadata expectations, and invalid-factor guard via pytest.raises; emit calibration metrics (JSON) into `${ARTIFACT_TS}/calibration_metrics.json`; capture pytest output with `KMP_DUPLICATE_LIB_OK=TRUE DBAT023_ARTIFACT_DIR=plans/active/DB-AT-023/reports/${ARTIFACT_TS} pytest -v tests/dbex/test_calibration_policy.py -k DB_AT_023 | tee plans/active/DB-AT-023/reports/${ARTIFACT_TS}/pytest_db_at_023.log`; run `pytest --collect-only tests -k DB_AT_023` (tee to collect log); after tests pass, update docs/TESTING_GUIDE.md §2, docs/development/TEST_SUITE_INDEX.md, and docs/spec-db-conformance.md calibration section with new selector status and artifacts; append Attempts History entry in docs/fix_plan.md.
Pitfalls To Avoid: - Keep photon conversion numerically stable (use float64 intermediate, cast back to float32); - Do not mutate canonical DataLoad arrays in tests—work on copies; - Validate `adu_per_photon` strictly positive before conversion and surface spec citation in error; - Preserve existing SCALE-002 post-simulation scaling semantics (no double scaling); - Maintain `[panel, slow, fast]` ordering when computing ROI differences; - Ensure tests skip gracefully when canonical assets missing; - Archive calibration_metrics.json with indent=2 deterministic ordering; - Respect Environment Freeze (no package installs); - Synchronize documentation only after selectors collect >0 tests; - Avoid leaking CLI flags into diffbragg backend path.
If Blocked: Capture failing command output under plans/active/DB-AT-023/reports/${ARTIFACT_TS}/blockers/, note blocker in docs/fix_plan.md Attempts History (include log path), and pause implementation pending supervisor guidance.
Findings Applied (Mandatory): CONFORMANCE-001 — keep DB_AT selector naming/env flags consistent; CONFIG-001 — honor ROI/mask geometry when threading conversion factors; SCALE-001 — avoid re-scaling structure factors during photon conversion; SCALE-002 — align with existing spot_scale_override handling; TESTING-003 — promote selector to Active only after collect-only artifact proves >0 tests.
Pointers: docs/spec-db-workflow.md:21; docs/architecture.md:175; docs/config_crosswalk.md:96; dbex/refine_one.py:20; dbex/nanobrag_bridge.py:57; plans/active/DB-AT-023/implementation.md:1; plans/active/DB-AT-023/reports/2025-11-04T052222Z/summary.md:1
Next Up (optional): 1) Extend calibration guard to surface representation metadata in parity harness once DB_AT_023 lands.
Doc Sync Plan (Conditional): After DB_AT_023 tests pass, rerun `pytest --collect-only tests -k DB_AT_023`, archive log, update docs/TESTING_GUIDE.md §2, docs/development/TEST_SUITE_INDEX.md, and docs/spec-db-conformance.md calibration section with selector status, env flags, and artifact paths.
Mapped Tests Guardrail: Ensure `pytest --collect-only tests -k DB_AT_023` reports ≥1 test; if it collects 0, author the missing acceptance tests or downgrade selector status before closing the loop.
