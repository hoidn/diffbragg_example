# Galph Iteration 42 Summary
**Date**: 2025-10-29 19:15:20 UTC
**Loop**: NANOBRAG-GOLDEN-001 — Replace fallback DB_AT_001 golden dataset
**Mode**: Parity
**Status**: Planning checkpoint (non-implementation dwell pass)

## Summary

Galph iteration 42 performed a reality check on the NANOBRAG-GOLDEN-001 initiative, discovering a critical gap: canonical tensor payloads (`.npy` files) are missing from the current repository despite manifest references to them. Previous captures (e.g., 2025-10-29T190533Z) ran in a sibling checkout (`/home/ollie/Documents/diffbragg_example_2/diffbragg_example_2/`) and never transferred the generated tensors to this workspace. The fixtures directory contains only `manifest.json` and `metadata.json`, while golden_dataset directories lack the expected `legacy/bragg_diffbragg.npy` and `torch/bragg_torch.npy` files. As a result, DB_AT_001 parity tests fall back to synthetic tensors and cannot validate canonical parity.

This loop staged a hardened follow-up implementation plan with generator safeguards and a new input.md directing the next actor (Ralph) to regenerate tensors within this checkout.

## Key Actions

1. **Reality validation** (planning_notes.md): Confirmed that `tests/fixtures/golden_data/simple_cubic/` lacks all `.npy` payloads despite manifest entries, while `plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T190533Z/golden_dataset/` (prior capture) also lacks tensor files.

2. **Manifest analysis**: Extracted generator command from manifest: `generator_command` points to `/home/ollie/Documents/diffbragg_example_2/diffbragg_example_2/...`, confirming prior capture ran from a different workspace.

3. **Finding registration**: Added MANIFEST-001 to `docs/findings.md:13`: "Canonical manifest emission MUST verify `.npy` payloads exist in the same workspace before writing checksums; otherwise manifests capture foreign paths and DB_AT_001 falls back to synthetic tensors."

4. **Attempt entry**: Logged 2025-10-29T191906Z checkpoint in `docs/fix_plan.md:35` with detailed diagnosis of manifest/payload gap and exit criteria still unmet.

5. **Planning document creation**: Generated `plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T191906Z/planning_notes.md` capturing reality checks, implications, and next focus areas.

6. **Input plan update**: Rewrote `input.md` with a ready-for-implementation Do Now:
   - Implement `scripts/generate_simple_cubic_golden.py::generate_simple_cubic_golden` with co-located tensor existence checks
   - Rerun canonical capture ensuring `.npy` payloads persist locally
   - Rerun DB_AT_001 parity validator and archive all evidence under the new timestamp
   - Hard validity contract targets checklist items A3/B1/B3/C1 from implementation.md

## Decisions/Rationales

**Dwell enforcement**: This is iteration 42's second consecutive planning pass for NANOBRAG-GOLDEN-001 (dwell≈15 per galph_memory.md:404). Per loop discipline, the third dwell would trigger `state=ready_for_implementation`. This loop pre-set that state in input.md to unblock the next actor.

**Manifest-001 finding**: Codified the requirement that generator functions must fail loudly (not silently emit manifests) if tensor files are missing post-write. This prevents silent protocol violations where manifests reference foreign checkouts.

**Artifact repointing**: Instructed next actor to direct all parity harness outputs to `2025-10-29T191906Z/parity_harness/`, ensuring new evidence supersedes stale logs from 2025-10-29T190533Z.

**Implementation floor avoided**: Loop discipline forbids issuing two docs-only passes in a row for the same focus; iter-42 honored this by preparing a Do Now with code tasks (generator safeguards + tensor check) rather than extending docs planning.

## Errors/Failures

**No execution errors detected** in this planning loop. However, systematic failure is evident:

- **Manifest/payload desynchronization**: Generator wrote full-provenance manifest but never persisted `.npy` tensor files to the current checkout, violating the implicit contract that `generate_simple_cubic_golden.py --fixtures` copies tensors to the fixtures directory.
- **Suspected root cause**: Prior capture (2025-10-29T190533Z) ran in a different workspace and used `--fixtures tests/fixtures/golden_data/simple_cubic` with a symlinked or relative path that resolved to the sibling checkout. When manifest was committed to integration branch, tensor files were not part of the commit, leaving manifests "orphaned" in the current repo.

## Evidence/Links

**Planning Notes**:
- `plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T191906Z/planning_notes.md` — Reality checks confirming missing `.npy` files and foreign manifest path

**Updated Documentation**:
- `docs/findings.md:13` — MANIFEST-001 registered with failure mode and remediation
- `docs/fix_plan.md:35` — Checkpoint 2025-10-29T191906Z appended with detailed diagnosis
- `input.md` — Rewritten Do Now with hard validity contract for next iteration

**Manifest Analysis**:
- `tests/fixtures/golden_data/simple_cubic/manifest.json` — References `.npy` payloads not present locally
- `plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T190533Z/golden_dataset/manifest.json` — Generator command references `/home/ollie/Documents/diffbragg_example_2/diffbragg_example_2/...`

**Exit Criteria Status**:
- Exit criterion A3 (canonical tensors present in fixtures): **UNMET** (only manifest/metadata, no `.npy` files)
- Exit criterion B1/B3 (manifest with full provenance, fixtures refreshed): **PARTIALLY MET** (manifest exists but payloads missing)
- Exit criterion C1 (DB_AT_001 selector Active): **BLOCKED** (parity harness falls back to synthetic tensors without canonical payloads)

## Next Steps

1. **Ralph iteration 43**: Execute the Do Now from updated `input.md`:
   - Add tensor existence checks to `scripts/generate_simple_cubic_golden.py::generate_simple_cubic_golden`
   - Run canonical capture within this checkout: `PYTHONPATH=../nanoBragg/src:$PYTHONPATH KMP_DUPLICATE_LIB_OK=TRUE python scripts/generate_simple_cubic_golden.py --canonical-out plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T191906Z/golden_dataset --emit-manifest --fixtures tests/fixtures/golden_data/simple_cubic --hkldebug ...`
   - Verify `.npy` files exist in fixtures post-capture
   - Rerun DB_AT_001 parity smoke test and archive evidence under new timestamp
   - Update `docs/fix_plan.md` entry status to complete/blocked depending on outcome

2. **Follow-up investigation** (if thresholds remain unmet):
   - If parity metrics (correlation, localization) remain poor after successful tensor generation, escalate to parity-debug loop per `docs/spec-db-tracing.md` first-divergence workflow
   - Document any residual intensity discrepancies (e.g., sum_ratio < 1.0) as findings for downstream scale refinement

3. **Documentation sync**: Once tensor regeneration completes in a new workspace with documented provenance, refresh `docs/findings.md` with resolution notes for MANIFEST-001 and update `docs/TESTING_GUIDE.md` with the latest DB_AT_001 selector status.
