# Galph Iteration 46 Summary

**Date**: 2025-11-03 (log: 23:52:37–00:02:01 UTC)
**Status**: Planning complete, parity offset analysis documented
**Focus**: NANOBRAG-GOLDEN-001 — Replace fallback DB-AT-001 golden dataset

## Summary

Galph iteration 46 performed planning and diagnostic analysis on the NANOBRAG-GOLDEN-001 initiative following completion of iteration 45's ROI triptych implementation. The core finding: canonical dataset generation (iteration 45) executed in a different workspace (`/home/ollie/Documents/diffbragg_example_2/diffbragg_example`), leaving ROI `.npz` payload files absent from the active checkout while manifest records point to out-of-repo paths. Galph recomputed masked parity metrics directly from fixtures and identified a systematic spatial peak offset between torch and DiffBragg outputs (example: roi_idx=5 shows DiffBragg peak at (6,6) vs torch peak at (11,7)), with global correlation remaining -0.0041 and localization success rate at 5.6%. Updated fix plan with new Attempts History entry and prepared Do Now instructions for ralph iteration 46 to patch peak-offset logging and regenerate canonical tensors locally.

## Key Actions

1. **Validated missing ROI payloads**
   Confirmed that `plans/active/NANOBRAG-GOLDEN-001/reports/2025-11-03T233556Z/roi_triptychs/` contains only `index.json` (no `.npz` bundles), and both manifest files record generator commands rooted in `/home/ollie/Documents/diffbragg_example_2/diffbragg_example`, proving canonical capture ran in a different workspace.

2. **Recomputed masked parity metrics from fixtures**
   Extracted `bragg_diffbragg.npy`, `bragg_torch.npy`, and `loss_mask_panel_0.npy` from `tests/fixtures/golden_data/simple_cubic/` and computed:
   - Global correlation: -0.0041 (masked regions only)
   - Median ROI correlation: -0.0358
   - Localization success rate: 5.6%
   - Torch nonzero pixels: ~5.6M; DiffBragg nonzero: ~97k

3. **Identified systematic spatial offset**
   Inspected sample ROI (roi_idx=5, bbox 715:727 × 429:441) and observed:
   - DiffBragg peak: (6,6) with ~1.0e3 photons
   - Torch peak: (11,7) with ~2.4e2 photons
   - Transpose/flip analysis confirmed offset is spatial misalignment, not axis inversion

4. **Updated fix plan with parity probe entry**
   Added new Attempts History row (2025-11-04T000201Z) documenting missing payloads, manifest paths, measured correlations, peak displacements, and next actions (peak-offset logging, canonical regeneration, parity smoke rerun).

5. **Prepared Do Now instructions for ralph iteration 46**
   Authored `input.md` with specific tasks:
   - Patch `scripts/generate_simple_cubic_golden.py::compute_roi_metrics` to emit per-ROI torch/diff peak coordinates and filenames
   - Regenerate canonical dataset using local paths with `--roi-dump` argument
   - Run DB_AT_001 parity smoke test with artifact collection

## Decisions/Rationales

- **Continue on NANOBRAG-GOLDEN-001**: Initiative remains in_progress pending parity offset investigation. Fixture correlation metrics (-0.0041 global, 5.6% localization) remain below DB_AT_001 acceptance threshold but ROI triptychs from iteration 45 provide foundation for first-divergence debugging per spec-db-tracing.md workflow.

- **Workspace isolation**: Canonical tensors must be regenerated inside this checkout to satisfy MANIFEST-001 finding (no foreign repo paths in manifests) and maintain local parity traceability. Prior iteration's out-of-repo generation was acceptable for diagnostics but blocks exit criteria enforcement.

- **Peak-offset logging as prerequisite**: Before tightening DB_AT_001 thresholds or investigating torch detector geometry, per-ROI peak metadata enrichment (coordinates, displacements, filenames) is required per docs/spec-db-tracing.md:12 first-divergence workflow to avoid speculative fixes.

- **Preserve SCALE-002 state**: Torch post-simulation scaling (√scale_override) must remain unchanged during regeneration; iteration 45 already corrected the double-scaling issue, and this iteration focuses on geometric/peak alignment diagnostics.

## Errors/Failures

**No execution errors occurred.** All diagnostic probes (numpy array inspection, index.json parsing, correlation computations) succeeded. Data mismatches observed are root causes, not failures:

1. **Root Cause: Manifest path mismatch**
   - Manifest `generator_command` points to `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/...` (different workspace)
   - Local `.npz` payloads never written because generation ran elsewhere
   - Suspected cause: iteration 45 executed with PYTHONPATH/script pointing to sibling checkout

2. **Root Cause: Torch vs DiffBragg geometric misalignment**
   - Example ROI shows 5-pixel peak offset in both y and x (DiffBragg (6,6) → torch (11,7))
   - Transposition/flip combinations do not correct alignment
   - Suspected cause: torch detector model (beam center, basis vectors) or coordinate order mismatch during ROI extraction/slicing

3. **Root Cause: Magnitude imbalance**
   - Torch sum(masked pixels) = 80k, DiffBragg sum = 917k (11× ratio)
   - Torch nonzero count much higher (~5.6M) than DiffBragg (~97k)
   - Suspected cause: torch simulator output sparsity model differs from DiffBragg (background treatment, threshold application)

## Evidence/Links

- **Log file**: `logs/integration/galph/iter-00046_20251103_235237.log` (lines 600–2348)
- **Fixture correlation check**: Lines 1138–1199 (numpy array recomputation from `tests/fixtures/golden_data/simple_cubic/`)
- **Manifest path inspection**: Line 733 (grep for "diffbragg_example_2" in scripts)
- **Index.json parsing errors**: Lines 643–703 (KeyError on missing 'filename' field, discovery of out-of-repo payload references)
- **Fix plan update**: Lines 1914–1949 (insertion of 2025-11-04T000201Z Attempts History entry)
- **Input.md preparation**: Lines 1960–2072 (Do Now instructions with patch targets, regeneration steps, validation selector)
- **Updated galph_memory.md**: Line 2210 (dwell=19, state=ready_for_implementation, next_action=log_peak_offsets_regenerate_and_run_parity)
- **Analysis notes**: `plans/active/NANOBRAG-GOLDEN-001/reports/2025-11-04T000201Z/analysis_notes.md` (captured during iteration)
- **Git commits**: Automatic 4-file commit at line 2222 (189081 bytes to reports/integration), sync state updated to galph=complete

## Next Steps

1. **Ralph iteration 46 (engineer phase)**:
   - Implement peak-offset logging in `scripts/generate_simple_cubic_golden.py::compute_roi_metrics`
   - Regenerate canonical dataset with `--roi-dump` using local paths
   - Run DB_AT_001 parity smoke and capture logs

2. **Galph iteration 47 (supervisor planning)**:
   - Review ralph's roi_offsets.json and peak metadata from iteration 46
   - Decide: investigate torch detector geometry (beam center calibration) vs invoke external torch maintainer consultation
   - If offsets documented and consistent, advance to PARITY-DEBUG phase or mark NANOBRAG-GOLDEN-001 blocked pending geometry clarification

3. **Documentation sync**:
   - Update `docs/findings.md` if new durable lessons emerge (e.g., torch coordinate conventions, detector-model sensitivity)
   - Synchronize `docs/prompt_sources_map.json` if new internal references added
   - Consider archiving completed checklist items from `docs/fix_plan.md` if parity investigation yields closure
