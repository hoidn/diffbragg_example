# Iteration 107 — Galph Supervisor Planning Turn

## Summary

Galph completed a planning turn for the PERF-WARM-SIM-001 initiative (Warm Simulator; Eliminate Per-Iteration Re-Instantiation). Identified and documented a critical blocker: the benchmark script (`plans/active/PERF-WARM-SIM-001/bin/benchmark_stage_a_cache.py`) imports the installed `dbex` package instead of the workspace checkout, causing it to lack the newly added `enable_stage_a_warm_cache` config flag. Both warm and cold benchmark modes abort before emitting metrics, preventing exit criterion #1 (speedup evidence) from being satisfied. Prepared a refined Do Now handoff to fix the script's module path resolution, add cache-mode telemetry tagging, rerun benchmarks, and update findings documentation with measured speedup data.

## Key Actions

1. **Root Cause Analysis**: Confirmed benchmark script resolves installed `dbex` distribution, not the workspace source code containing new warm-cache toggle
2. **Telemetry Enhancement**: Planned addition of `cache_mode` string field ("warm"|"cold") to `RefinementTelemetry` for artifact attribution
3. **Script Path Fix**: Scheduled prepending of repo root to `sys.path` within benchmark script to ensure workspace import
4. **Documentation Updates**: Flagged updates needed to:
   - `docs/fix_plan.md` (added planning entry for 2025-11-06T095520Z)
   - `input.md` (refined Do Now with targeted fixes)
   - `galph_memory.md` (appended new supervisor turn record)

## Decisions/Rationales

**Decision**: Delay full benchmark execution until module path is fixed and cache-mode is properly instrumented in telemetry.

**Rationale**: Previous attempts to run benchmark script failed with `TypeError: __init__() got an unexpected keyword argument 'enable_stage_a_warm_cache'`. This indicates the script is importing an outdated package installation rather than the current workspace code. Adding explicit `sys.path` manipulation ensures the script sees new RefinementConfig fields.

**Telemetry Decision**: Add `cache_mode` to telemetry (default "warm") to disambiguate warm vs cold benchmark artifacts in retrospective analysis.

**Rationale**: Future iterations need to verify which code path executed; `cache_mode` tag in telemetry/HDF5 outputs eliminates ambiguity when reviewing old benchmark artifacts.

## Errors/Failures and Root Causes

**Error**: `TypeError: __init__() got an unexpected keyword argument 'enable_stage_a_warm_cache'`

**Context**: Occurred when attempting to execute `plans/active/PERF-WARM-SIM-001/bin/benchmark_stage_a_cache.py` against previously authored test case.

**Root Cause**: Script uses `import dbex` without explicit path setup, resolving to the installed package distribution (which lacks the new flag) instead of `/home/ollie/Documents/diffbragg_example/src/dbex/` (workspace checkout with recent changes).

**Status**: Identified but not yet fixed; Fix scheduled in Do Now handed to Ralph (engineer agent).

## Evidence/Links

**Log files and artifacts examined**:
- `plans/active/PERF-WARM-SIM-001/reports/2025-11-06T090721Z/summary.md` — Previous planning turn noting exit criterion #1 remains unmet
- `plans/active/PERF-WARM-SIM-001/reports/2025-11-06T081209Z/` — Telemetry implementation artifacts (perf counters added)
- `plans/active/TORCH-REFINE-002/reports/2025-11-05T041539Z/pytest_stage_a.log` — Baseline Stage A runtime (184.55s) for comparison
- `plans/active/PERF-WARM-SIM-001/reports/2025-11-06T081209Z/pytest_stage_a.log` — Warm-cache Stage A runtime (175.22s)

**Code references**:
- `dbex/nanobrag_refinement.py:321` — `RefinementTelemetry` dataclass (target for cache_mode field)
- `dbex/nanobrag_refinement.py:592` — Stage A warm/cold branch point for telemetry tagging
- `plans/active/PERF-WARM-SIM-001/bin/benchmark_stage_a_cache.py:1` — Benchmark script header requiring sys.path fix
- `tests/dbex/test_torch_refine_smoke.py:370` — Stage A smoke test assertions (target for cache-mode validation)
- `docs/findings.md:45` — PERF-WARM-001 knowledge base entry (target for speedup evidence)

**File changes in this iteration**:
- `docs/fix_plan.md` — Added new planning entry (2025-11-06T095520Z)
- `input.md` — Updated Do Now with refined task list and artifact paths
- `galph_memory.md` — Appended supervisor turn entry with focus state and next actions

## Next Steps

1. **Ralph (Engineer)** to execute refined Do Now from `input.md`:
   - Add `cache_mode` string field to `RefinementTelemetry` dataclass with default "warm"
   - Set `cache_mode` inside `run_nanobrag_refinement` when constructing Stage A telemetry
   - Prepend repo root to `sys.path` in benchmark script before importing `dbex`
   - Execute warm and cold benchmark runs with isolated logs
   - Update `docs/findings.md::PERF-WARM-001` with measured timings and speedup ratio

2. **Validation gates**:
   - Stage A smoke test must pass: `pytest tests/dbex/test_torch_refine_smoke.py::test_stage_a_expansion`
   - Benchmark script must produce JSON + text summaries under `plans/active/PERF-WARM-SIM-001/reports/2025-11-06T095520Z/`
   - Measured speedup evidence must be recorded in `docs/findings.md` with artifact paths

3. **Follow-up planning**:
   - Once Stage A benchmark evidence is captured and exit criterion #1 satisfied, extend warm-cache and perf counter telemetry to Stage B shell modifiers
   - Document any performance improvements or regressions in telemetry structure
