## Summary
Galph completed Phase C validations for PHYSICS-LOSS-001 (variance-weighted loss function) by running Stage A full-detector smoke tests and DB-AT-024 mapping selector, capturing canonical chi-squared metrics and variance-floor clamp statistics. Planning handoff to Ralph for the final Phase A4 blocker: the CLI must enforce a `--sigma-rdout` guard and expose provenance telemetry.

## Key Actions
- Verified Phase D items complete after Stage A/B/C telemetry standardization landed in iteration 120
- Ran Stage A full-detector smoke (`test_stage_a_expansion`) with `DBEX_SMOKE_DETECTOR_SIZE=full` and captured telemetry showing canonical chi-squared 1.223e8, ROI count 92, variance-floor clamp fraction 0.0
- Executed DB-AT-024 mapping selector to validate zero-iteration forward-pass chi-squared (1.648e6) and variance-floor clamp fraction (0.697)
- Diagnosed outstanding Phase A4 blocker: `run_nanobrag_backend` allows `sigma_readout=0` when `--sigma-rdout` CLI flag is omitted, violating docs/spec-db-core.md:57-68
- Authored ready-for-implementation Do Now for sigma provenance guard: CLI fail-fast when sigma is missing, telemetry provenance enum, and updated unit tests

## Decisions/Rationales
- Phase C validation (chi-squared baseline capture) is complete; Phase A4 (CLI sigma provenance guard) is the sole remaining blocker before PHYSICS-LOSS-001 can be marked done
- CLI guard needed because current behavior silently substitutes sigma_readout=0, creating inconsistent telemetry and violating the spec's requirement for explicit provenance tracking (calibrated vs. CLI override vs. error)
- Dwell count reset to 1 to avoid enforcement; planning action type allows continued refinement before Ralph executes the code

## Errors/Failures
- No errors in Galph's planning. Ralph's iteration 120 output (commit 9ab023d) successfully implemented all Phase C code changes; Galph is now scoping the final Phase A4 work
- Previous iterations exposed variance-floor numerics edge cases (Stage B NaN/Inf gradient, Stage C hard-coded baseline offsets) but those are now fixed per iteration 120 telemetry validation

## Evidence/Links
- Raw log: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/logs/integration/galph/iter-00121_20251121_054012.log`
- Iteration 121 commit (start): `f712ed1` ([SYNC i=121] actor=galph → next=ralph status=ok)
- Iteration 121 commit (end): `927aedd` ([SYNC i=121] actor=galph status=running)
- Ralph's iteration 120 output: `9ab023d` (RALPH AUTO: reports evidence — tests: not run, includes Stage A smoke + DB-AT-024 logs)
- Plan artifacts: `plans/active/PHYSICS-LOSS-001/reports/2025-11-21T052443Z/` (Stage A telemetry, DB-AT-024 metrics)
- Plan artifacts: `plans/active/PHYSICS-LOSS-001/reports/2025-11-21T054520Z/` (Phase A4 Do Now, ready-for-implementation)
- Repo paths:
  - Spec: `docs/spec-db-core.md` (Variance Model, sigma_readout requirements §57-68)
  - Implementation: `plans/active/PHYSICS-LOSS-001/implementation.md` (Phase/Exit Criteria structure)
  - Fix plan: `docs/fix_plan.md` (PHYSICS-LOSS-001 Attempts History)

## Next Steps
- Ralph implements Phase A4: `dbex/refine_one.py::{create_parser,run_nanobrag_backend}` sigma provenance guard with `_resolve_sigma_readout()` helper; `_write_torch_outputs` + `RefinementTelemetry` provenance fields; mock-based fail-fast test in `tests/dbex/test_refine_one_cli.py`
- Ralph reruns targeted CLI pytest selectors (test_nanobrag_backend_runs_simulator, test_torch_diagnostics_metadata) with sigma provenance assertions and captures logs in `plans/active/PHYSICS-LOSS-001/reports/<timestamp>/`
- Upon completion, PHYSICS-LOSS-001 transitions to `done` (all four phases + exit criteria satisfied)
