# Iteration 00160 Summary

**Timestamp**: 2025-11-22 12:38:24 UTC
**Commit**: c6ed76f `[SYNC i=160] actor=galph status=running`

## Summary

Supervisor iteration 160 reviewed Ralph's Phase C2 convergence test execution for TORCH-GEOMETRY-PARITY-002 (quaternion U-matrix parameterization). While the zero-point parity check passed with perfect accuracy (CC=0.9999999843), the Phase 5 convergence tests catastrophically failed with CC degenerating to -0.045 and chi-squared exploding to 1.43B after just 5 Adam optimization steps. A shape mismatch bug in B_ideal_reciprocal computation was diagnosed and fixed, but the fundamental issue was identified: the quaternion SO(3) constraint is incompatible with the mapping geometry which contains ~0.06% volume scaling (`det(U₀)=1.000557`). This triggers escalation to TORCH-GEOMETRY-PARITY-003 to investigate the root cause and implement alternative parameterizations.

## Key Actions

- Reviewed Ralph's Phase C2 execution artifacts showing decisive partial success at zero deltas but catastrophic convergence failure during optimization
- Diagnosed root cause of RuntimeError: `cctbx_cell(...).fractionalization_matrix()` returns 1D array, not 3x3 matrix; `.reshape(3,3)` fix applied
- Analyzed parity vs convergence dichotomy: raw U-matrix achieves perfect parity (3.5e-18) but det(U)≠1 causes SO(3) projection to degrade parity to ~4e-05
- Determined quaternion parameterization is not viable; confirmed convergence improvement hypothesis is false
- Escalated to TORCH-GEOMETRY-PARITY-003 with focus on det(U)≠1 root cause (dxtbx A*/cell inconsistency hypothesis) and hybrid cell+U+scale parameterization design
- Updated galph_memory.md with decision rationale and next actions

## Decisions/Rationales

1. **Alternative Path 3 Failed**: Ralph chose to test convergence despite ~4e-05 parity gap (pragmatic sensitivity test), but optimization crashed with CC→-0.045. This decisively proves quaternion U-matrix is not viable.

2. **Escalation Trigger**: Per repeat-failure rule, TORCH-GEOMETRY-PARITY-003 created to investigate: (H1) dxtbx A*/cell inconsistency causing det(U)=1.000557, (H2) physical volume scaling, (H3) numerical precision errors. Root cause determines parameterization strategy.

3. **Next Parameterization**: Hybrid cell+quaternion+isotropic-scale factorization proposed (Option 1): `A* = s * quaternion_to_matrix(q) * cell_matrix`, where isotropic scale absorbs det offset, quaternion enforces SO(3), and cell parameters remain as baseline.

## Errors/Failures

**Phase C2 Convergence Test Failure**:
- Error: `RuntimeError: size mismatch, got input (3), mat (3x3), vec (9)` at line 434
- Root cause: `cctbx_cell(...).fractionalization_matrix()` returns flat 9-element row-major array; `.T` transpose on 1D array is no-op
- Fix: Added `.reshape(3, 3)` before transpose at line 325 to create proper (3,3) torch tensor
- Symptom: After bugfix applied, convergence test executed but produced CC=-0.045 and chi²=1.43B (catastrophic failure)

**Quaternion Parameterization Viability**:
- Zero-point validation: CC=0.9999999843, perfect parity (3.5e-18), chi²_rel_diff=-0.017% — logic is correct at zero
- Phase 5 optimization: A_scale_only variant degraded immediately (5 steps → CC=-0.045), indicating fundamental incompatibility
- Root cause: Quaternion parameterization via `scipy.spatial.transform.Rotation.from_matrix()` enforces SO(3) constraint, projecting `det(U₀)=1.000557` → `det(U_init)=1.0`, losing ~0.06% volume scaling present in mapping MOSFLM A*
- Consequence: Parameterization cannot represent true mapping geometry; gradient flow likely inverted or singular

## Evidence/Links

- **Phase C2 Execution Report**: `/home/ollie/Documents/diffbragg_example/plans/active/TORCH-GEOMETRY-PARITY-002/reports/2025-11-22T120500Z/`
- **Phase C1 Parity Failure Diagnosis**: `/home/ollie/Documents/diffbragg_example/plans/active/TORCH-GEOMETRY-PARITY-002/reports/2025-11-22T113409Z/phase_c1_parity_failure_diagnosis.md`
- **Zero-Point Check Output**: Zero-point validation passed with CC=0.9999999843, chi²_rel_diff=-0.017%
- **Convergence Test Metrics**: A_scale_only collapsed from CC≈1.0 → CC≈-0.045 after 5 Adam steps; chi² → 1.43B
- **Shape Bug Fix**: `plans/active/TORCH-GEOMETRY-PARITY-002/reports/2025-11-22T120500Z/phase_c2_shape_bug_diagnosis.md`
- **Supervisor Memory**: `/home/ollie/Documents/diffbragg_example/galph_memory.md` (2025-11-22T120500Z entry)

## Next Steps

1. **Create TORCH-GEOMETRY-PARITY-003** initiative with three-phase plan (Phase A: root cause investigation via dxtbx audit, Phase B: parameterization design evaluation, Phase C: implement chosen hybrid approach)
2. **Phase A Actions**: Audit dxtbx `crystal.get_A()` vs `crystal.get_unit_cell()` alignment; check if MOSFLM/DIALS applied isotropic scale; validate `det(A*)` vs `1/det(B_ideal)` relationship; determine if det offset is calibration artifact or physical
3. **Phase B Design**: Evaluate hybrid cell+quaternion+scale vs quaternion+anisotropic-scale vs GL(3)+regularization; prototype gradient flow for chosen factorization
4. **Phase C Implementation**: Extend parity probes for new parameterization; run Phase 5 convergence tests expecting CC ≥0.99, stable χ²; validate regression guard
5. **Rollback Decision**: If hybrid parameterization fails, may need to accept cell+misset with documented 1.37e-3 strain limitation and escalate to future detector/mapping calibration audit (DXTBX-002)
