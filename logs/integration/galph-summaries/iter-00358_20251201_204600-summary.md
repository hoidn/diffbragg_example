# Iteration 358 Summary (Galph)

**Timestamp**: 2025-12-01 15:58:11 UTC
**Focus**: ARCH-REFINE-001 Phase E.3
**Status**: Complete
**Next Actor**: Ralph (iteration 359)

## Summary

Galph planned and scoped Phase E.3 of ARCH-REFINE-001, focusing on extracting the Stage B baseline parity guard from `_run_stage_b_lbfgs` into a standalone helper function to enable unit testing without constructing a full optimizer. The test harness was identified as the blocker: `test_stage_b_baseline_guard_diff_payload` failed due to missing optimizer dependencies in mock setup. Galph refined the implementation plan with explicit code extraction tasks, test rewrite instructions, and validation commands, preparing Ralph for implementation.

## Key Actions

- **Analyzed test failure**: Confirmed that `tests/dbex/test_stage_b_cpu_fallback.py::test_stage_b_baseline_guard_diff_payload` fails because `_run_stage_b_lbfgs` requires a fully populated `param_values` dict with optimizer handle, tensors, and Stage B mode before the REFINE-FLOW-001 diff writer can execute.
- **Reality check completed**: Determined that as long as the parity guard remains inline within `_run_stage_b_lbfgs`, the JSON schema and telemetry field stability cannot be verified independently.
- **Refactoring plan drafted**: Scoped the extraction of lines 1105-1210 from `dbex/refinement/stage_b_impl.py::_run_stage_b_lbfgs` into a module-private helper that accepts canonical baseline snapshot, Stage B initial χ², telemetry dict, param_values subset, `compute_loss_stage_b`, and `n_panels`.
- **Test rewrite specified**: Updated `test_stage_b_baseline_guard_diff_payload` to import the new helper instead of `_run_stage_b_lbfgs`, supply minimal `param_values` subset (canonical tensors + cache metadata), and exercise both parity fail (verify RuntimeError + diff schema + telemetry path) and parity pass paths (diff path stays `None`).
- **Validation commands prepared**: Provided exact pytest commands with environment variables (`DBEX_SMOKE_DETECTOR_SIZE=small`, `DBEX_SMOKE_SIGMA_SOURCE=cli_override`, `DBEX_SMOKE_TELEMETRY_PATH=`) for guard unit test and Stage B/C small-detector smoke tests.
- **Documentation updated**: Updated `docs/fix_plan.md` with Phase E.3 scope (lines 2020-2049), added validation command block with telemetry JSON collection, and marked Phase E.2 complete in `plans/active/ARCH-REFINE-001/implementation.md`.
- **Input file refreshed**: Rewrote `input.md` with the Phase E.3 Do Now, mapped pytest selectors, How-To Map, pitfalls, and findings pointers for Ralph to follow.
- **Memory updated**: Logged the planning loop in `galph_memory.md` with timestamp, focus, dwell count (0), action type (planning), and state (ready_for_implementation).

## Decisions/Rationales

- **Extract guard into helper**: Extracting the parity check/diff writer into a standalone helper allows unit testing the REFINE-FLOW-001 guard deterministically without spinning up a full LBFGS optimizer, reducing test complexity and improving observability.
- **CPU-only test**: Kept device/dtype neutral for the refactored test so the guard can be validated on CPU hardware without allocating shell modifiers, reducing test infrastructure dependencies.
- **Panel-mode validation**: Forced panel-mode evaluation during the parity check (per PERF-WARM-009) to prevent Stage B ROI sampling from masking baseline mismatches.
- **Telemetry field stability**: The Stage B/C small-detector smoke bundle explicitly asserts `stage_b_baseline_rel_diff ≤ 1e-6` to prove the refactor keeps telemetry fields stable post-extraction.
- **Dwell reset**: Set dwell=0 because this is an implementation-focused planning loop with explicit code tasks and validation selectors prepared for delegation.

## Errors/Failures

- **test_stage_b_baseline_guard_diff_payload FAILED**: Test fails before reaching the guard logic due to `KeyError: 'optimizer'` in the mock setup. This is the root cause driving Phase E.3 refactoring.
- **Suspected root cause**: The current test harness calls `_run_stage_b_lbfgs` directly, which expects a complete optimizer context that the mock cannot provide. The fix requires factoring out the guard logic into a standalone helper that accepts only the minimal parameters required for baseline parity validation.

## Evidence/Links

- **Implementation plan**: `plans/active/ARCH-REFINE-001/implementation.md` (Phase E.3 scope marked ready for implementation)
- **Fix plan ledger**: `docs/fix_plan.md:781-810` (Phase E.3 full scope with commands and env vars)
- **Test under scrutiny**: `tests/dbex/test_stage_b_cpu_fallback.py::test_stage_b_baseline_guard_diff_payload`
- **Source to refactor**: `dbex/refinement/stage_b_impl.py::_run_stage_b_lbfgs` (lines 1105-1210)
- **Smoke test selectors**: `tests/dbex/test_torch_refine_smoke.py -k "test_stage_b_shell_modifiers or test_stage_c_detector_microslip"`
- **Previous report**: `plans/active/ARCH-REFINE-001/reports/2025-12-01T160850Z/` (Phase E.2 completion with Stage B/C smoke validation)
- **Spec references**: REFINE-FLOW-001 (1e-3 relative tolerance), PERF-WARM-009 (panel-mode validation during parity check)
- **Telemetry spec**: `stage_b_baseline_rel_diff` and `stage_b_baseline_abs_diff` fields expected in telemetry dict

## Next Steps

- **Implement helper extraction**: Extract the parity check/diff writer from `_run_stage_b_lbfgs` (lines 1105-1210) into a module-private helper that records `stage_b_baseline_rel_diff/abs_diff` on telemetry, emits `stage_b_baseline_diff.json` when `|Δ| > 1e-3`, and raises RuntimeError citing REFINE-FLOW-001.
- **Rewrite CPU fallback test**: Update `test_stage_b_baseline_guard_diff_payload` to import the new helper, supply minimal `param_values` subset, and exercise both parity fail and parity pass paths with JSON schema validation.
- **Run guard unit test**: Execute `pytest -vv tests/dbex/test_stage_b_cpu_fallback.py::test_stage_b_baseline_guard_diff_payload` to verify the refactored guard raises RuntimeError + diff schema on parity failure and stays silent on parity pass.
- **Run Stage B/C smokes**: Execute the small-detector smoke bundle with telemetry collection to prove the refactor keeps `stage_b_baseline_rel_diff ≤ 1e-6` and Stage C telemetry untouched.
- **Capture artifacts**: Store collect-only + pytest logs + telemetry JSON under `plans/active/ARCH-REFINE-001/reports/2025-12-01T161600Z/` (or new timestamp if loop extends).
- **Verify exit criteria**: Confirm helper exists, is unit-tested, guard raises with JSON payload in isolation, and Stage B/C smokes pass with panel-mode baseline parity (stage_b_baseline_rel_diff ≤ 1e-6).
