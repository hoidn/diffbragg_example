# Iteration 363 Summary

## Summary
Galph captured definitive Stage C telemetry evidence showing both detector sizes (small/full) now run panel-mode closures yet still regress chi² by 0.063–0.067% despite achieving 99.99999% detector-offset reduction. Identified root cause in REFINE-012's "force panel closures" logic that disables ROI closures, and prepared a targeted fix: restore ROI-mode closures when Stage A telemetry reports ROI mode while keeping validations panel-scoped, plus add a new `validation_scope` perf-counter field.

## Key Actions
- Ran `summarize_stage_c_warm_cache.py` on 2025-12-01T173200Z telemetry to capture warm-cache behavior
- Analyzed warm-cache report and confirmed panel-mode closures are active yet chi² regression persists
- Inspected `dbex/refinement/stage_c_impl.py` to trace the interaction between `_build_stage_c_params` and the `force_panel_validation` guard
- Drafted ROI-closure restoration plan: drop `and not force_panel_validation` guard, compute explicit `validation_scope` field, update Stage C smoketest assertions
- Rewrote `input.md` with detailed Do Now: implement ROI-closure fix in `stage_c_impl.py::_build_stage_c_params` and `_run_stage_c_lbfgs`, update test assertions, rerun small/full Stage C detector smokes with new telemetry output path (2025-12-01T174500Z)

## Decisions/Rationales
- **Root cause identified**: REFINE-012 forced all panel-mode closures to suppress ROI sampling; this removed the warm-cache benefit and caused chi² regression despite offset reduction
- **Fix approach chosen**: Keep ROI closures active (gated on Stage A telemetry `roi_mode`) while maintaining separate `validation_scope` perf-counter field for audit and compliance
- **Telemetry strategy**: Include both `roi_mode` and new `validation_scope` fields in perf counters so downstream tooling can track closure vs validation scope alignment per REFINE-011/012
- **Test assertion updates**: Extend `test_stage_c_detector_microslip` to verify both closure mode (`roi_mode`) and validation scope are recorded correctly

## Errors/Failures
- No blocking errors encountered; log shows successful planning completion
- Stage C chi² regression (0.063–0.067%) was the evidence being gathered, not an error—this is now documented and motivates the ROI-closure restoration

## Evidence/Links
- `logs/integration/galph/iter-00363_20251201_173205.log` — This iteration's full log
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T173200Z/stage_c_warm_cache_report.json` — Warm-cache telemetry showing chi² regression with panel closures
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T173344Z/stage_c_panel_inspection.md` — Inspection summary motivating this telemetry loop
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T173344Z/summary.md` — Turn summary artifact
- `docs/fix_plan.md:989-1022` — Updated PERF-WARM-SIM-001 Phase D.4 notes with ROI-closure restoration plan
- `docs/findings.md:60,65,72-73` — REFINE-007 (chi² gate), REFINE-010 (panel closure logic), REFINE-011/012 (closure/validation scope alignment)
- `dbex/refinement/stage_c_impl.py:178-340` — Current Stage C ROI-mode and `force_panel_validation` logic
- `tests/dbex/test_torch_refine_smoke.py:990-1250` — Stage C smoketest assertions and `_record_stage_telemetry` call site

## Next Steps
- Ralph to implement ROI-closure fix: patch `dbex/refinement/stage_c_impl.py::_build_stage_c_params` and `_run_stage_c_lbfgs` to restore ROI closures while keeping validations panel-scoped
- Update `tests/dbex/test_torch_refine_smoke.py::test_stage_c_detector_microslip` to assert both `roi_mode` and new `validation_scope` perf-counter fields
- Run small-detector Stage C smoketest with telemetry and confirm selector health
- Execute full-detector Stage C smoketest with telemetry collection
- Re-run `summarize_stage_c_warm_cache.py` on new 2025-12-01T174500Z telemetry to verify REFINE-007 chi² gate passes with ROI closures restored
- If Stage C still regresses after ROI fix, prepare follow-up plan to adjust LBFGS hyperparameters or document REFINE-007 gate recalibration backed by evidence
