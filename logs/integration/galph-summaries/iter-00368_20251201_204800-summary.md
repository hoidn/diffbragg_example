# Iteration 368 Summary (2025-12-01T21:11:28Z)

## Summary

Galph completed a planning iteration focused on PERF-WARM-SIM-001 (Warm Simulator performance optimization). Analysis identified a critical ROI-mode mismatch in Stage C where LBFGS closure optimization operates on a 92-ROI minibatch while the REFINE-007 chi-squared gate validates panel-wide (all detectors). This population divergence reproduces a consistent +0.067% regression on the full-detector test. The iteration produced a concrete alignment plan to disable ROI closures whenever Stage A telemetry enforces panel validations, preserving provenance through a new `roi_mode_reason="validation_scope_panel"` marker.

## Key Actions

- **Evidence Review**: Analyzed 2025-12-01T190945Z telemetry artifacts showing Stage C reports `roi_mode="roi"` while Stage A advertises `validation_scope="panel"`
- **Root Cause Identified**: Mismatch between optimization population (92-ROI minibatch) and validation population (panel-wide) causing reproducible +0.067% chi-squared regression
- **Spec Alignment**: Verified spec requirement (docs/spec-db-workflow.md) that optimization and validation populations must match once Stage A disables ROI mode
- **Plan Formulated**: Created Phase D.4 plan to gate `stage_c_roi_mode_active` on `not force_panel_validation` with new provenance tracking
- **Documentation Updated**: Modified docs/fix_plan.md and input.md to reflect new ROI-mode alignment strategy
- **Memory Recorded**: Added 2025-12-01T193800Z entry to galph_memory.md with planning state and next action

## Decisions/Rationales

- **Disable ROI closures when Stage A forces panel validations**: This aligns the optimization population with what REFINE-007 measures, eliminating the population divergence that causes the +0.067% regression
- **Preserve provenance**: Adding `roi_mode_reason="validation_scope_panel"` allows telemetry-driven debugging and maintains REFINE-012 audit trail requirements
- **Telemetry-driven logic**: Avoid hard-coded detector-size checks; instead, read Stage A's validation scope from telemetry and propagate to Stage C closure parameters
- **Validation scope**: Keep `validation_scope` and `roi_mode_label` consistent (panel when either ROI disabled or validations forced per Stage A)

## Errors/Failures

- None in this planning iteration. The previous implementation (2025-12-01T190945Z) had identified the param_deltas recording issue as a separate blocker, but this iteration focuses on the underlying population-mismatch problem discovered during telemetry analysis.

## Evidence/Links

- `docs/fix_plan.md:1146` - New Phase D.4 entry describing ROI-mode alignment plan and artifact path
- `docs/spec-db-workflow.md` - Optimization Strategy section requiring closure population to match validation population
- `dbex/refinement/stage_c_impl.py:180-220` - Stage C parameter builder where `stage_c_roi_mode_active` and `roi_mode_reason` are defined
- `tests/dbex/test_torch_refine_smoke.py:954` - Stage C detector microslip smoketest enforcing REFINE-007/011/012 gates
- `galph_memory.md:2025-12-01T190945Z` - Previous planning entry with baseline-prior ordering analysis
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T190945Z/` - Telemetry artifacts showing roi_mode="roi" with validation_scope="panel" mismatch
- `input.md` - Updated handoff to Ralph with ROI-mode gating implementation task

## Next Steps

1. Implement: Modify `dbex/refinement/stage_c_impl.py::_build_stage_c_params` (~lines 180-230) to gate `stage_c_roi_mode_active` on `not force_panel_validation`, add `roi_mode_reason="validation_scope_panel"` branch, and sync perf counters
2. Validate: Re-run `tests/dbex/test_torch_refine_smoke.py::test_stage_c_detector_microslip` for both small and full detector sizes with telemetry capture
3. Summarize: Execute warm-cache summarizer on new telemetry to confirm `roi_mode="panel"`, detector-offset reductions ≥99.999%, and chi² regression ≤0.05%
4. Artifact Path: Collect logs and telemetry under `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T193800Z/`
5. Success Criteria: Full-detector telemetry must report `perf_counters['roi_mode']=="panel"` and `roi_mode_reason=="validation_scope_panel"` alongside ≤0.05% chi² regression
