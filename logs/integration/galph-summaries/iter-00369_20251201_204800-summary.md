# Iteration 369 Summary (Galph Supervisor Loop)

## Summary

Galph completed a planning-phase loop focused on PERF-WARM-SIM-001 (Warm Simulator), analyzing why Stage C refine still regresses chi-squared despite prior snapshot/telemetry fixes (REFINE-013). Root cause identified: a guard disables ROI closures whenever Stage A forces panel validations, removing the deterministic ROI minibatch that enabled LBFGS convergence. The loop produced an updated fix-plan entry and detailed `input.md` for Ralph to re-enable ROI closures while preserving panel-mode validation scope, with reproducible test commands and telemetry collection paths.

## Key Actions

- Reviewed canonical documentation: `docs/index.md`, `docs/fix_plan.md`, `docs/findings.md` (findings REFINE-007/010/012/013, PERF-WARM-006/007)
- Analyzed latest telemetry under `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T193800Z/` confirming flat chi-squared traces (210848512.0 constant across LBFGS iterations)
- Diagnosed divergence from November PASS artifacts: only remaining delta is the `and not force_panel_validation` clause disabling ROI closures
- Documented new fix-plan entry (§2025-12-01T200900Z) in `docs/fix_plan.md` with concrete implementation plan
- Rewrote `input.md` with full Do Now for Ralph: drop ROI-closure guard, preserve `validation_scope="panel"`, update test assertions
- Generated precise test commands with environment variables and artifact capture paths for both small and full detector configurations
- Updated `galph_memory.md` with focus state, dwell time, and next-action instructions

## Decisions/Rationales

- **Root Cause Clarity**: REFINE-013 rehydration + baseline-prior ordering fixes guarantee best snapshots are restored before telemetry, yet chi-squared remains flat. The only remaining delta vs. working November builds is that ROI-mode closures were gated by `force_panel_validation`, preventing the deterministic ROI minibatch from being used during LBFGS optimization.
- **Decouple ROI Mode from Validation Scope**: ROI closures (optimization population) and validation scope (telemetry validation population) are independent and must be asserted separately. The guard was conflating these two orthogonal concerns.
- **Evidence-Driven Next Step**: Re-enable ROI closures (drop the guard) while keeping `validation_scope="panel"` for full validations, then re-run both smoketests to verify REFINE-007 gates (chi-squared regression ≤0.05%, detector-offset reduction ≥80%).
- **Comprehensive Telemetry**: Capture collect-only, pytest, and warm-cache summarizer artifacts at both detector sizes to provide complete evidence for supervisor review if Stage C still fails.

## Errors/Failures

No new errors generated in this planning loop. Previous work context:
- **Repeat-failure guard triggered**: Full detector test produces identical +0.067% chi-squared regression (Stage A final=2.1071e+08, Stage C final=2.1085e+08) as 2025-12-01T170326Z loop, with flat LBFGS trace indicating panel-mode closures prevent convergence.
- **Suspected root cause**: ROI-closure guard (introduced 2025-12-01T170326Z) removes deterministic ROI minibatch when Stage A forces panel validations, flattening the optimization landscape.

## Evidence/Links

Repository-relative paths referenced in log:

- `docs/index.md` — Authoritative project documentation index
- `docs/fix_plan.md` — Updated with new entry §2025-12-01T200900Z
- `docs/findings.md` — Prior findings (REFINE-007/010/012/013)
- `docs/TESTING_GUIDE.md` — Selector specifications and test guidance
- `dbex/refinement/stage_c_impl.py:160-320` — ROI-mode logic and perf-counter wiring (target for modification)
- `dbex/refinement/stage_c_impl.py:742-754` — REFINE-013 rehydration logic (do not modify)
- `tests/dbex/test_torch_refine_smoke.py:1188-1230` — Perf-counter assertions for `roi_mode` and `validation_scope`
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T193800Z/` — Prior loop telemetry showing repeat failure
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T200900Z/` — New artifact directory for test/telemetry collection
- `galph_memory.md` — Updated with loop state and next-action guidance

## Next Steps

- Ralph implements `dbex/refinement/stage_c_impl.py::_build_stage_c_params`: drop `and not force_panel_validation` clause so ROI closures follow Stage A telemetry again
- Update `roi_mode_reason` provenance to only report why ROI mode is disabled (warm cache off, Stage A panel mode, no ROIs)
- Ensure test assertions in `tests/dbex/test_torch_refine_smoke.py::test_stage_c_detector_microslip` assert `roi_mode` and `validation_scope` independently
- Execute full test suite per commands in `input.md` for both detector sizes (small/full) with telemetry capture under `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T200900Z/`
- If chi-squared regression persists >0.05% after ROI closures re-enabled, escalate to supervisor with full telemetry/summarizer output for gate recalibration decision
- If tests pass: proceed to next PERF-WARM-SIM-001 phase (post-REFINE-013 validation)

