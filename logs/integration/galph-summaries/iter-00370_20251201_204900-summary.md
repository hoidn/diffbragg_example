# Iteration 370 Summary (Galph Supervisor)

## Summary

Galph completed iteration 370 as a planning-focused supervisory loop staying on focus PERF-WARM-SIM-001 (Stage C warm-cache detector offset validation). The loop analyzed why Stage C's chi² regresses by +0.067% despite detector offsets recovering >99.99999%, traced the root cause to `dbex/refinement/stage_c.py` reusing post-tanh Euler angles instead of the raw `orientation_vec` tensor from Stage A telemetry, and logged the fix as REFINE-014 in `docs/findings.md`. No production code changes landed; the loop produced planning artifacts and updated `docs/fix_plan.md` and `input.md` to direct Ralph to the Stage C orientation tensor restoration work.

## Key Actions

- Reviewed latest telemetry from `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T200900Z/telemetry_stage_c_full.json` showing persistent +0.067% chi² regression despite 99.99999% offset reduction
- Inspected `dbex/refinement/stage_c.py:174-355` to understand Stage C baseline divergence from Stage A
- Identified root cause: Stage C wrapper feeds `param_deltas['misset_xyz_deg']['delta']` (clamped Euler angles) to `_build_stage_c_lbfgs_closure` instead of raw `orientation_vec`, causing double tanh application that collapses misset toward zero
- Logged REFINE-014 finding in `docs/findings.md:75` with detailed evidence and fix scope
- Updated `docs/fix_plan.md` with new fix-plan entry (line 1246) scoping orientation tensor handoff repair
- Updated `input.md` with Ralph's next Do Now: rebuild Stage C orientation tensor from `param_deltas['orientation_vec']['final']` and rerun both Stage C smoketests with telemetry capture

## Decisions/Rationales

- **Stayed on PERF-WARM-SIM-001**: No pivot; the regression root cause is now clear and actionable within the warm-cache validation scope
- **Planning action over implementation**: Supervisor loop focused on evidence review and root-cause diagnosis; no code edits issued because the analysis needed completion before direction to Ralph
- **Localized touch to Stage C wrapper**: The fix targets only `dbex/refinement/stage_c.py::StageC.run()` and does not touch `_build_stage_c_lbfgs_closure` or production refinement logic
- **Telemetry-driven gate validation**: Both Stage C smoketests (small + full) will rerun under `2025-12-01T204500Z` directory with the `summarize_stage_c_warm_cache.py` script; REFINE-007 gate requires ≤0.05% chi² regression with ≥80% offset reduction

## Errors/Failures

No critical errors in this loop. The +0.067% chi² regression (documented in telemetry_stage_c_full.json) remains unresolved but is now attributed to a clear implementation bug: Stage C applies tanh twice to the orientation misset, collapsing it toward zero and breaking Stage C baseline alignment with Stage A final telemetry.

## Evidence/Links

- `logs/integration/galph/iter-00370_20251201_214745.log` — Full supervisor loop execution
- `docs/findings.md:75` — REFINE-014 logging the orientation double-tanh bug with code citations
- `docs/fix_plan.md:1246` — New fix-plan entry for Stage C orientation restoration (Active status)
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T200900Z/telemetry_stage_c_full.json` — Evidence of persistent chi² regression despite offset recovery
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T200900Z/pytest_stage_c_full.log` — Test run output for the 2025-12-01T200900Z loop
- `dbex/refinement/stage_c.py:174-355` — Stage A telemetry reconstruction and Stage C initialization (double-tanh site)
- `dbex/refinement/stage_c_impl.py:178-213` — Stage C LBFGS closure builder and ROI-mode decision logic

## Next Steps

- Ralph (iteration 371) will implement Stage C orientation tensor restoration:
  - Build frozen orientation tensor from `stage_a_telemetry['param_deltas']['orientation_vec']['final']`
  - Pass restored tensor through `param_values['orientation_vec']` to `_build_stage_c_lbfgs_closure`
  - Keep `misset_deg_for_crystal` sourced from existing Euler deltas for telemetry continuity
- Rerun both Stage C detector microslip smoketests (small + full) with telemetry capture under `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T204500Z/`
- Validate REFINE-007 gate: χ² regression ≤0.05% with ≥80% detector-offset reduction in both telemetry files

