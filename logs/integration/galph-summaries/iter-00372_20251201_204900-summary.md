# Iteration 372 Summary

## Summary

Galph diagnosed a persistent +0.067% Stage C chi-squared regression relative to Stage A as a trusted-mask mismatch. Stage A's panel-mode validations intersect the ROI/loss mask with the DIALS trusted mask before computing variance-weighted loss, but Stage C's closure never applies this masking, resulting in chi-squared calculations that integrate pixels Stage A already excludes. Galph updated docs/fix_plan.md with REFINE-016 findings and prepared a concrete Do Now for Ralph to implement the trusted-mask gate in Stage C loss computation before rerunning detector microslip smokes.

## Key Actions

- **Root-cause analysis**: Traced persistent +0.067% Stage C chi² drift by comparing Stage A (`dbex/refinement/stage_a_impl.py:1336-1365`) and Stage C (`dbex/refinement/stage_c_impl.py:512-593`) loss computation paths; found Stage A masks pixels with DIALS trusted mask but Stage C does not.
- **Evidence capture**: Reviewed telemetry from `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T210900Z/telemetry_stage_c_full.json` showing Stage A final chi²=2.10706464e+08 vs Stage C baseline chi²=2.10848512e+08 even with zero detector offsets.
- **Documentation update**: Added REFINE-016 entry to docs/findings.md and updated docs/fix_plan.md (lines 1343-1357) with trusted-mask parity Do Now including stage references and test commands.
- **Input.md preparation**: Detailed Ralph's next loop with exact commands for Stage C detector microslip smokes (small/full sizes), telemetry collection, and warm-cache summarizer invocation under new artifact directory `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T214200Z/`.
- **Artifact generation**: Created `trusted_mask_analysis.md` under the report directory documenting the masking mismatch evidence and fix strategy.
- **Memory log update**: Recorded supervisor findings and Do Now in galph_memory.md with action state marked as "ready_for_implementation".

## Decisions/Rationales

- **Focus**: Stayed in Parity mode (planning turn) to unblock Ralph by scoping the implementation without blocking further analysis.
- **Root cause**: Masking mismatch selected over prior hypotheses (log-scale baseline, orientation misalignment) after evidence showed Stage C baseline already 0.067% above Stage A even before optimization.
- **Implementation scope**: Isolated fix targets `_build_stage_c_lbfgs_closure` to apply trusted masks in both ROI-mode and panel-mode loss paths, avoiding in-place tensor mutation per PHYSICS-LOSS constraints.
- **Artifact path**: New timestamped directory (`2025-12-01T214200Z`) ensures docs/fix_plan references remain valid and separates this loop's telemetry from prior attempts.
- **Command environment**: Preserved `NANOBRAGG_DISABLE_COMPILE=1` and `KMP_DUPLICATE_LIB_OK=TRUE` flags from prior Stage C smoke runs to maintain warm-cache state.

## Errors/Failures

- **No new runtime errors surfaced**. Prior four Stage C loops failed with repeatable +0.067% chi² regression signature despite detector offsets collapsing 99.99999%, indicating systematic baseline mismatch rather than optimization divergence.
- **Suspected root causes eliminated**: Log-scale baseline fix (REFINE-015) and orientation double-tanh correction (REFINE-014) both landed without resolving the chi² drift, narrowing focus to masking path.

## Evidence/Links

- **Root cause documentation**: `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T214200Z/trusted_mask_analysis.md` (mask mismatch analysis)
- **Stage A reference**: `dbex/refinement/stage_a_impl.py:1336-1365` (trusted-mask gate in panel validations)
- **Stage C target**: `dbex/refinement/stage_c_impl.py:512-593` (loss computation requiring mask fix)
- **Telemetry baseline**: `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T210900Z/telemetry_stage_c_full.json` (chi² baseline evidence)
- **Docs updates**: `docs/fix_plan.md:1343-1357` (PERF-WARM-SIM-001 Do Now section), `docs/findings.md` (new REFINE-016 entry)
- **Test suite**: `tests/dbex/test_torch_refine_smoke.py::test_stage_c_detector_microslip` (canonical smoke targets for validation)

## Next Steps

1. **Ralph implementation**: Apply trusted-mask AND gate in `_build_stage_c_lbfgs_closure` for both ROI-mode and panel-mode branches; thread `stage_a_ctx.trusted_masks_t` (warm path) or tensorized `inputs.trusted_mask` (cold path) before `_compute_variance_weighted_loss`.
2. **Validation commands**: Run Stage C detector microslip smokes with both detector sizes, capturing collect logs, pytest logs, telemetry JSON, and warm-cache summarizer output.
3. **Verification gate**: Confirm stage_c_warm_cache_report.json shows chi² regression now ≤0.05% per REFINE-007 and detector offset reductions remain ≥80%.
4. **Archive artifacts**: Store all new telemetry, summarizer output, and analysis under `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T214200Z/`.
5. **Optional follow-up**: After masking parity is confirmed green, investigate ROI sampling tuning to potentially regain Stage C performance margins.
