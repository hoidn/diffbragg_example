# Iteration 373 Summary

## Summary

Galph (supervisor agent) completed a comprehensive diagnostic and planning analysis of the PERF-WARM-SIM-001 initiative, identifying that Stage C's panel-mode loss computation diverges from Stage A's canonical implementation, causing a repeatable +0.067% chi-squared regression. The root cause is not the trusted-mask gating (already implemented in REFINE-016) but rather duplicated/divergent loss logic between Stage A and Stage C. Planning produced a detailed helper refactor specification to unify the panel-loss computation, with comprehensive Do Now instructions and pitfall guidance ready for Ralph's implementation.

## Key Actions

- Diagnosed Stage C chi-squared regression: Full-detector telemetry shows Stage A baseline at 2.1071e+08 while Stage C consistently records 2.1085e+08 despite zero detector offsets
- Confirmed trusted-mask hypothesis was incorrect: All trusted-mask tensors are 100% True, but REFINE-016 gating implementation was retained as correct per spec
- Analyzed telemetry structure and Stage A/C parameter flow to isolate remaining divergence in panel-mode loss computation
- Extracted and documented the exact refactor pattern: extract `compute_loss` panel branch (stage_a_impl.py:1450) into a reusable helper, then replace Stage C's duplicated logic with a call to that helper
- Updated `docs/fix_plan.md` Tier 3 status and appended detailed Phase D.4 planning entry with full implementation checklist
- Updated `galph_memory.md` with new focus state (ready_for_implementation, dwell=1)
- Created input.md with complete Do Now specification including refactor steps, test commands, warm-cache summarizer invocation, and detailed pitfalls to avoid
- Reserved artifact path: `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T221500Z/`

## Decisions/Rationales

- **Kept REFINE-016 trusted-mask implementation**: Although it didn't resolve the test regression, the gating is correct per spec and testing revealed the fixture has all-True masks, confirming the mask logic itself wasn't the blocker
- **Pivot from mask parity to panel-loss parity**: Concluded after analyzing telemetry and code that Stage C's re-implementation of panel-mode loss (separate from reusing Stage A's) is the real source of drift
- **Refactor-only approach (no weakening tolerances)**: Chose to unify the code path rather than relax REFINE-007's 0.05% acceptance gate, maintaining spec rigor
- **Preserve warm-cache semantics**: Designed the helper to maintain simulator reuse and StageAContext liveness, avoiding cold-path performance regressions during refactoring

## Errors/Failures

- **REFINE-007 chi-squared gate failure** (repeating across iterations 2025-12-01T163900Z through 2025-12-01T210900Z): Stage C full-detector smoke test fails with +0.067% chi-squared regression vs. Stage A baseline (210706464.0 vs 210848512.0), unchanged by REFINE-015 (param_deltas fix) and REFINE-016 (trusted-mask gating). Root cause identified as panel-loss computation divergence rather than gating logic.
- **No new implementation errors in this loop**: Loop was planning-focused and produced no code changes, only diagnostic/design output

## Evidence/Links

- `logs/integration/galph/iter-00373_20251201_224508.log` (this log)
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T214200Z/trusted_mask_analysis.md` (root-cause diagnostic from previous loop, confirmed)
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T214200Z/telemetry_stage_c_full.json` (telemetry showing stage_a_final_chi2=210706464.0 vs consistent Stage C validations at 210848512.0)
- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T214200Z/telemetry_stage_c_small.json` (small detector passed REFINE-007, showing ROI-mode branch is correct)
- `dbex/refinement/stage_a_impl.py:1450` (Stage A panel-mode loss block to extract)
- `dbex/refinement/stage_c_impl.py:512-618` (Stage C duplicated panel-mode logic to replace with helper call)
- `docs/spec-db-workflow.md:62` (canonical requirement: Stage C shall evaluate the same loss population as Stage A before detector offsets apply)
- `docs/TESTING_GUIDE.md:12` (canonical env knobs for smoke tests)

## Next Steps

- **Ralph (implementation)**: Land the Stage A/C panel-loss helper refactor and rerun Stage C detector microslip smoke tests for both small and full detector sizes with telemetry capture
  1. Extract `_compute_panel_loss_from_context` helper from `stage_a_impl.py:1450` block
  2. Update `compute_loss` to call the new helper (no behavior change)
  3. Replace Stage C's panel-mode branch in `stage_c_impl.py::_build_stage_c_lbfgs_closure` with helper call after detector retargeting
  4. Run collect-only and pytest logs for both detector sizes to `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T221500Z/`
  5. Capture telemetry JSON for both sizes and run warm-cache summarizer to validate chi-squared parity
  6. Success criterion: Stage C chi² matches Stage A within REFINE-007 tolerance (≤0.05% regression)

- **Future work (if refactor succeeds)**: Add per-panel chi-squared telemetry to Stage C to catch future baseline drift regressions
