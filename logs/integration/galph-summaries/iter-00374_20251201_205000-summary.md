# Iteration 374 Summary (Galph Supervisor Loop)

## Summary

Galph (supervisor) transitioned focus from ARCH-REFINE-001 architecture refactoring to PERF-WARM-SIM-001 warm-simulator optimization after confirming that Stage A/C panel-loss helpers are now unified. The key finding from the prior implementation is that Stage C still exhibits a persistent +0.067% chi² offset relative to Stage A despite sharing the same loss computation helper. Rather than modifying physics or acceptance gates, Galph scoped a new diagnostics instrumentation phase to capture per-panel chi², mask, and sigma metrics from both Stage A and Stage C, enabling pinpoint identification of where divergence originates.

## Key Actions

- **Focus transition**: Marked ARCH-REFINE-001 complete after confirming Stage A/C panel-loss helper extraction merged successfully; identified Stage A final chi² baseline at 2.10706464e+08.
- **Diagnostic plumbing design**: Scoped instrumentation of `dbex/refinement/stage_a_impl.py::_compute_panel_loss` and `dbex/refinement/stage_c_impl.py` with optional per-panel diagnostics collection gated by env var `DBEX_STAGE_C_PANEL_DIAG_DIR`.
- **Comparison tool specification**: Designed new script `plans/active/PERF-WARM-SIM-001/bin/compare_panel_diag.py` to ingest Stage A and Stage C panel-diagnostics JSON pairs, align by panel_id, and report chi² deltas and checksum mismatches.
- **Test infrastructure**: Specified detailed validation commands to re-run Stage C detector microslip smoketests (both small and full detector sizes) with diagnostics enabled, outputting per-panel metrics to JSON under `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T223500Z/panel_diag/`.
- **Documentation updates**: Updated `docs/fix_plan.md` Attempts History and `input.md` (Do Now, How-To Map, Findings Applied) to guide Ralph (engineer agent) through diagnostics implementation.

## Decisions/Rationales

- **Evidence-driven approach**: Rather than speculatively adjusting REFINE-007 tolerance gates or tweaking physics, Galph directed collection of finer-grained diagnostics (per-panel chi², mask/sigma checksums) to identify the root cause of the 0.067% offset.
- **Reuse existing helper vs. deeper instrumentation**: Previous loop confirmed that extracting `_compute_panel_loss` and having Stage C call it did not eliminate the chi² drift, meaning the issue stems from upstream data/telemetry state rather than duplicated panel logic; new phase focuses on observable differences via diagnostics.
- **Env-gated diagnostics over hot-path changes**: Chosen to keep diagnostics opt-in (via `DBEX_STAGE_C_PANEL_DIAG_DIR`) so default warm-cache fast-path remains untouched and no production codepath is perturbed by instrumentation.
- **Separate detector-size reporting**: Each smoketest run writes diagnostics to a unique subdirectory (`panel_diag/small` and `panel_diag/full`) so metrics remain separable and repeatable.

## Errors/Failures

No execution errors encountered in this supervisor loop. The prior Ralph (engineer) loop successfully merged the shared `_compute_panel_loss` helper but left the root-cause chi² drift unresolved; Galph now addresses this with targeted diagnostics rather than further code changes.

## Evidence/Links

- `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T221500Z/` — Prior loop artifacts showing Stage A final chi²=2.10706464e+08 vs. Stage C full chi²=2.10848512e+08 (delta 0.067%).
- `docs/fix_plan.md` — Attempts History entry at 2025-12-01T223500Z documenting Phase D.4 panel-loss diagnostics instrumentation plan.
- `input.md` — Complete Do Now assignment with How-To Map, test commands, pitfalls, and findings (REFINE-011/012/013/016, PERF-WARM-006/013, DIAGNOSTICS-001).
- `dbex/refinement/stage_a_impl.py:970-2100` — Stage A closure/telemetry helpers to be extended with optional diagnostics collection.
- `dbex/refinement/stage_c_impl.py:300-980` — Stage C closure and runner where diagnostics plumbing mirrors Stage A.
- `plans/active/PERF-WARM-SIM-001/bin/summarize_stage_c_warm_cache.py` — Existing warm-cache summarizer that new diagnostics JSON will complement.

## Next Steps

1. **Ralph implementation**: Execute Do Now to add optional `panel_diag_collector` parameter to `_compute_panel_loss`, thread it through Stage A/C helpers, and create `compare_panel_diag.py` comparison tool.
2. **Test execution**: Re-run Stage C detector microslip smoketests (small + full) with `DBEX_STAGE_C_PANEL_DIAG_DIR` set to diagnostics output directory.
3. **Evidence collection**: Run warm-cache summarizer and comparison tool to produce JSON/Markdown reports of per-panel chi² deltas.
4. **Root-cause analysis**: Use per-panel diagnostics to identify whether divergence is mask-related, sigma-related, tensor-stacking order, or data-flow upstream of panel-loss computation.
5. **Follow-up initiative**: If diagnostics pinpoint a correctable issue, open a dedicated follow-up loop; if root cause requires spec clarification, escalate to spec-change initiative per protocol.
