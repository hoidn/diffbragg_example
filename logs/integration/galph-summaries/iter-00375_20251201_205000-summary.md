# Iteration 375 Summary

**Date**: 2025-12-01T233918Z
**Agent**: Galph (Supervisor/Planner)
**Branch**: integration
**Status**: Complete

## Summary

Galph analyzed panel diagnostics from the previous iteration (2025-12-01T223500Z) showing that Stage C detector offset deltas collapse correctly (≤1.5e-08 mm) yet chi² remains +0.067% above Stage A—uniform across all panels. The root cause is identified: `_retarget_stage_a_detectors` updates `Detector` objects without rebuilding cached nanobrag `Simulator` instances, so warm-cache evaluations ignore the new geometry. Galph planned and documented a Phase D.4 implementation loop to rebuild simulators after detector retargeting, then validated via Stage C smoketests.

## Key Actions

- Reviewed panel diagnostics evidence showing detector offsets collapse but chi² stagnates
- Diagnosed that `_retarget_stage_a_detectors` swaps detector attributes without rebuilding cached nanobrag `Simulator` instances
- Updated `docs/fix_plan.md` with Phase D.4 follow-up plan (READY FOR IMPLEMENTATION)
- Updated `input.md` with concrete Do Now for Ralph: rebuild warm-cache simulators + rerun Stage C smoketests
- Prepared four-step implementation plan:
  1. Patch `dbex/refinement/stage_c_impl.py::_retarget_stage_a_detectors` to rebuild nanobrag `Simulator` after applying distance delta
  2. Preserve tensor-based distance deltas (GRADIENT-004) and Stage A context wiring
  3. Rerun `test_stage_c_detector_microslip` for both small and full detector sizes
  4. Compare panel diagnostics before/after to confirm χ² parity (≤0.05% regression)
- Committed 4 files (~270 KB) to integration branch

## Decisions/Rationales

- **Root Cause Focus**: Panel diagnostics prove detector offsets are correct in isolation; the divergence is in simulator warmth, not physics/gates
- **Simulator Rebuild Approach**: Instead of retaining only detector swaps, rebuild the full `Simulator` object using Stage A's cached beam config and HKL tensors to keep tensors hot while binding updated detector geometry
- **GRADIENT-004 Constraint**: Keep distance deltas as differentiable tensors; do not call `.item()` on arithmetic to preserve gradient flow through retarget+optimizer path
- **ROI Entry Simulators**: Update both per-panel simulators and ROI-entry simulators together so closures read identical geometry
- **Validation Strategy**: Reuse existing diagnostics tooling from previous loop; only the implementation target changes (simulator rebuild vs. instrumentation)

## Errors/Failures

None reported. Analysis was diagnostic and planning-focused; no implementation attempted yet.

## Evidence/Links

- **Panel Diagnostics Report**: `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T223500Z/` (evidence showing uniform chi² deltas and offset telemetry)
- **Target Module**: `dbex/refinement/stage_c_impl.py:1-110, 440-520, 612-704` (detector retarget helper, warm-cache closure, simulator wiring)
- **Reference Implementation**: `dbex/refinement/stage_a_impl.py:300-420` (Stage A context construction and simulator instantiation pattern)
- **Specifications**: `docs/spec-db-workflow.md §62-75` (Stage B/C sequencing and REFINE-007/011/012 guardrails)
- **Updated Plan Doc**: `docs/fix_plan.md:1425-1435` (Phase D.4 entry)
- **Updated Input**: `input.md` (complete Do Now + How-To Map + Pitfalls + Findings Applied)

## Next Steps

- Ralph to implement warm-cache simulator rebuild in `_retarget_stage_a_detectors`:
  - Instantiate fresh nanobrag `Detector` and `Simulator` after computing `new_distance_mm`
  - Replace `stage_a_ctx.simulators[pid]` with new simulator
  - Update any ROI-entry simulators with matching `panel_id`
  - Call `_retarget_stage_a_simulators` to sync shared crystal
- Run Stage C detector microslip smoketests (small and full) with diagnostics env knobs
- Post-process telemetry with `summarize_stage_c_warm_cache.py` and `compare_panel_diag.py`
- Verify chi² parity (≤0.05% regression) while detector-offset telemetry shows ≥99.999% reduction
- If blocked, escalate to spec-change or deeper callchain investigation

## Mandatory Specifications/Constraints

- **REFINE-007**: Stage C must not increase χ² by >0.05% once detector offsets collapse
- **REFINE-011/012**: Panel validations stay in sync with Stage A telemetry; ROI mode remains provenance-tracked
- **REFINE-013**: Best snapshot persistence relies on accurate full validations; simulator rebuilds cannot disrupt tuple flow
- **REFINE-016**: Trusted-mask parity in `_compute_panel_loss` must keep working after simulator replacement
- **GRADIENT-004**: Distance deltas must remain differentiable tensors through retarget+optimizer path

---

**Artifact Path**: `plans/active/PERF-WARM-SIM-001/reports/2025-12-01T230800Z/` (reserved for Ralph's implementation loop)
