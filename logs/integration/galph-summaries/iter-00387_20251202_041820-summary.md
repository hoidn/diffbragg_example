# Summary
Galph completed planning for ARCH-STAGE-CONTEXT-001 Phase B.3.2, scoping the migration of Stage B and Stage C telemetry from anonymous dicts to typed dataclasses. This extends the Stage A dataclass refactoring completed in the prior loop and produces a concrete handoff brief for Ralph with mapped smoketests, compatibility requirements, and validation gates.

# Key Actions
- Reviewed ARCH-STAGE-CONTEXT-001 findings: Stage B/C helpers still mutate anonymous dicts in `_build_stage_b_params`, `_run_stage_b_lbfgs`, `_build_stage_c_params`, and `_run_stage_c_lbfgs`, violating the architecture constraint documented in `docs/findings.md:83`.
- Updated `plans/active/ARCH-STAGE-CONTEXT-001/implementation.md` to mark B3.1 (Stage A telemetry dataclass) as complete and explicitly scope B3.2 (Stage B/C dataclasses).
- Updated `docs/fix_plan.md` with new planning artifact (lines 1629-1662) including detailed validation command templates.
- Logged loop state in `galph_memory.md:366-370` (state=ready_for_implementation, dwell=1).
- Created report stub at `plans/active/ARCH-STAGE-CONTEXT-001/reports/2025-12-02T083500Z/summary.md`.
- Produced complete `input.md` handoff with FocusItem, implementation scope, validation smoketests, pitfalls, and findings citations.

# Decisions/Rationales
- **Dataclass Scope**: Introduce `StageBTelemetryState` and `StageCTelemetryState` mirroring Stage A structure to replace dict mutations and enforce typed telemetry storage per ARCH-STAGE-CTX-001.
- **Compatibility Shims**: Keep `isinstance(telemetry_state, dict)` guards so archived tooling that expects dict semantics continues working during the CLI monolith migration.
- **Variance-Floor Preservation**: Maintain mutable list wrappers (e.g., `iteration_count = [0]`) as dataclass attributes to keep closure mutation-by-reference behavior intact; avoid copying tensors that would break cache reuse.
- **Panel Diagnostics Hook**: Expose `panel_loss_diag` on the dataclass so `DBEX_STAGE_C_PANEL_DIAG_DIR` instrumentation remains functional for PERF-WARM-SIM-001 evidence capture.
- **Schema Stability**: Ensure `RefinementTelemetry` HDF5 output schema and `StageBArtifacts` baseline fields remain unchanged; tests depend on exact key names.
- **Known Failures as Gates**: Stage B per-reflection (existing gradient-flow defect) and Stage C full-detector (+0.067% χ² regression per PERF-WARM-SIM-001) are expected to fail; any deviation is treated as regression.

# Errors/Failures
- **Fatal submodule error during sync**: "No url found for submodule path 'src/nanobrag-torch' in .gitmodules" (line 5152). This did not block the planning iteration but appears to be a pre-existing configuration issue unrelated to this loop's work.

# Evidence/Links
- Log file: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/logs/integration/galph/iter-00387_20251202_041820.log`
- Implementation plan: `plans/active/ARCH-STAGE-CONTEXT-001/implementation.md` (lines 5074-5076 updated B3.2 scope)
- Fix plan entry: `docs/fix_plan.md` (lines 1629-1662)
- Galph memory: `galph_memory.md` (lines 366-370)
- Handoff brief: Embedded in log output as complete `input.md` (lines 5089-5141)
- Architecture finding: `docs/findings.md:83` (ARCH-STAGE-CTX-001 — Stage helpers must stop mutating anonymous dicts)
- Stage A completion checkpoint: `plans/active/ARCH-STAGE-CONTEXT-001/reports/2025-12-02T073800Z/`
- Artifact directory for B3.2 validation: `plans/active/ARCH-STAGE-CONTEXT-001/reports/2025-12-02T083500Z/`

# Next Steps
- Ralph to implement `StageBTelemetryState` and `StageCTelemetryState` dataclasses in `dbex/refinement/context.py` with mutable list wrappers and docstring citations to ARCH-STAGE-CTX-001 + PHYSICS-LOSS-001/002.
- Rewire `dbex/refinement/stage_b_impl.py` and `dbex/refinement/stage_b.py` to instantiate and mutate the Stage B dataclass while preserving compatibility shims and `StageBArtifacts` baseline fields.
- Rewire `dbex/refinement/stage_c_impl.py` and `dbex/refinement/stage_c.py` to adopt the Stage C dataclass and maintain the `DBEX_STAGE_C_PANEL_DIAG_DIR` hook.
- Run mapped smoketests (Stage B shell PASS, Stage B per-reflection known failure, Stage C small PASS with panel diagnostics, Stage C full with PERF-WARM-SIM-001 failure signature) and capture logs under artifact directory.
- Confirm no regression: Stage B per-reflection and Stage C full-detector failure modes must match their documented signatures (gradient defect + 0.067% χ² regression respectively); any new failure or unexpected success halts progress.
