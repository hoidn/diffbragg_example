## Summary
Galph planning loop scoped Phase C.5 for ARCH-BRIDGE-RESP-001, directing the elimination of Stage wrapper and reconstruction helper dependencies on the bridge's config/input re-exports. Refined the implementation plan to reflect the import migration scope and authored a ready-for-implementation Do Now with explicit file targets, pytest selectors, and parity-mode guardrails.

## Key Actions
- Cataloged lingering imports in `dbex/refinement/{stage_a.py,stage_a_impl.py,stage_b.py,stage_c.py}`, panel-loss helpers, `dbex/physics/forward.py`, and reconstruction fast paths that still consume `RefinementInputs` and config factories via `dbex.nanobrag_bridge` re-exports.
- Cross-checked CLI and stage smoke tests (`test_refine_one_cli.py`, `test_torch_refine_smoke.py`) to identify patch decorators still targeting the bridge module symbols.
- Updated `plans/active/ARCH-BRIDGE-RESP-001/implementation.md` to mark Phase C.1–C.4 complete and add Phase C.5 checklist item (import swap across stages and reconstruction).
- Logged the new milestone in `docs/fix_plan.md` attempt history under 2025-12-02T100041Z with full scope and artifact path.
- Authored comprehensive `input.md` with production code tasks, a How-To Map with env-guarded pytest commands, explicit pitfalls (avoid moving geometry helpers, preserve dtype/device neutrality, keep CLI mask tensor determinism), and blockers recovery steps.

## Decisions/Rationales
- **Phase C.5 scope:** Stage wrappers must import `RefinementInputs` and config builders directly from `dbex.refinement.{inputs,config_factories}` to allow retirement of the bridge re-export shim, but geometry helpers (`compute_baseline_misset_deg`, quaternion math, HKL grid) remain sourced from the bridge in this loop.
- **Parity mode:** Changes are import rewiring only; no behavioral or signature changes to configs or inputs; all smoketests must pass with identical telemetry, Bragg tensors, and loss curves.
- **Test retargeting:** CLI patches must be updated to intercept functions at their new import sites (`dbex.refinement.inputs`/`dbex.refinement.config_factories`) instead of the bridge, maintaining test validation coverage after the swap.
- **Environment freeze honored:** No package installs or `nanobrag_torch` edits; all changes confined to module paths under the workspace.

## Errors/Failures
No blocking errors or failures detected. Prior phases (C.1–C.4) completed successfully; Stage A/B/C small-detector and targeted CLI tests all PASSED in the last Ralph loop. The current loop is pure planning with no new implementation, so no test runs occurred.

## Evidence/Links
- Log file: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/logs/integration/galph/iter-00407_20251202_095835.log` (2602 lines, captures full Galph turn and Ralph transition).
- Planning artifacts: `plans/active/ARCH-BRIDGE-RESP-001/reports/2025-12-02T100041Z/` (reserved for next Ralph loop; contains input.md with Do Now, How-To Map, and pitfall guards).
- Updated plan: `plans/active/ARCH-BRIDGE-RESP-001/implementation.md:84-89` (Phase C.5 checklist).
- Fix plan ledger: `docs/fix_plan.md` entry at 2025-12-02T100041Z (full narrative of scope and cross-file dependencies).

## Next Steps
Ralph should execute Phase C.5 by:
1. Updating `dbex/refinement/stage_a_impl.py` and `stage_a.py` to import config factories and `RefinementInputs` from `dbex.refinement.config_factories`/`dbex.refinement.inputs`.
2. Updating `dbex/refinement/stage_b.py` and `stage_c.py` similarly, keeping geometry helpers sourced from the bridge.
3. Updating `dbex/refinement/reconstruction.py` lazy import blocks in `build_stage_a_bragg` and `build_stage_b_bragg` to use the new factories.
4. Retargeting `tests/dbex/test_refine_one_cli.py` patch decorators to match the new import sites.
5. Running the mapped smoketests and CLI selectors (Stage A/B/C small-detector + two nanobrag CLI tests) to validate parity.
6. Recording pytest logs and any telemetry JSON under the artifacts directory and confirming behavior is unchanged.

Once this lands and tests pass, the bridge re-export shim can be marked safe for eventual removal, unblocking Phase C.6.
