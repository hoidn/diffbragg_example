# Iteration 00417 Summary

## Summary

Planning loop for ARCH-TELEMETRY-001 Phase C.1 continuation, analyzing Stage C telemetry collector failures and defining Stage C collector migration requirements. Reviewed `pytest_stage_c_smoke.log` showing empty `loss_trace_sample` due to early finalization of telemetry before final observer events.

## Key Actions

- Reviewed Stage C smoketest failure logs at `plans/active/ARCH-TELEMETRY-001/reports/2025-12-03T190000Z/pytest_stage_c_smoke.log`
- Diagnosed root cause: `collector.finalize()` and `StageResult.to_legacy_dict()` called before final validation events recorded
- Updated `docs/fix_plan.md` with Stage C-specific findings (lines 90-107)
- Extended Phase C checklist in `plans/active/ARCH-TELEMETRY-001/implementation.md` with Stage C follow-ups (lines 84-94)
- Rewrote `input.md` with concrete Stage C implementation targets and test commands
- Created new artifact directory structure at `reports/2025-12-03T210000Z/`

## Decisions/Rationales

- **Defer collector finalization**: Move `collector.finalize()` and `StageResult.to_legacy_dict()` until after final validation completes, ensuring all observer events are captured before snapshot
- **Eliminate legacy dict plumbing**: Read telemetry/perf counters directly from collector/StageResult objects instead of intermediate `legacy_telemetry_dict` variable
- **Guarantee non-empty loss trace**: Add fallback in `_build_stage_c_lbfgs_closure` to emit at least one sample when LBFGS exits immediately, preventing smoketest gate failures
- **Single-source principle**: Enforce typed collectors as sole telemetry writers per ARCH-STAGE-CTX-001/002 findings

## Errors/Failures

- **Stage C test failure**: `test_stage_c_detector_microslip` reported empty `loss_trace_sample`
  - Root cause: Early snapshot of telemetry via `StageResult.to_legacy_dict()` before final validation events
  - Status: Identified and documented; requires implementation changes for fix
- **Stage B tests**: Both `test_stage_b_baseline_guard_diff_payload` and `test_stage_b_shell_modifiers` passed in prior iteration (2025-12-03T190000Z)

## Evidence/Links

- Log file: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/logs/integration/galph/iter-00417_20251202_123109.log`
- Failure artifact: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/plans/active/ARCH-TELEMETRY-001/reports/2025-12-03T190000Z/pytest_stage_c_smoke.log`
- Updated plan: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/plans/active/ARCH-TELEMETRY-001/implementation.md`
- Input handoff: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/input.md`
- Fix plan ledger: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/docs/fix_plan.md`

## Next Steps

1. **Implement Stage C collector fixes**:
   - Reorganize `_run_stage_c_lbfgs` finish block to call `collector.finalize()` only after all validations complete
   - Remove legacy dict plumbing and unpack `StageResult`/`StageCTelemetry` directly into `RefinementTelemetry`
   - Add fallback sample emission in `_build_stage_c_lbfgs_closure` when optimizer exits without iterations

2. **Update Stage C code paths**:
   - Route all validation scopes (baseline, periodic, final) through collector payload
   - Delete remaining dict-based telemetry references
   - Add helper to `StageCTelemetryCollector` if needed (e.g., `record_baseline_sample`)

3. **Validate with mapped test selectors**:
   - Run Stage B guard, Stage B shell smoke, and Stage C detector microslip with test commands in `input.md`
   - Capture logs to `reports/2025-12-03T210000Z/` via `tee`
   - Stop on first failure; preserve logs for supervisor review

4. **Proceed to Phase C.2**: Once Stage C telemetry is green, advance to writer refactoring (Phase C.2 per plan)
