# Ralph Iteration 32 Summary
**Date**: 2025-10-29 | **Iteration**: iter-00032 | **Focus**: NANOBRAG-GOLDEN-001 — Canonical dataset generation with scale instrumentation

## Summary

Ralph iteration 32 focused on restoring the canonical DB_AT_001 golden dataset to enable non-zero nanobrag_torch panel outputs. The core task involved instrumenting the structure factor generation code to propagate the DiffBragg global scale factor into the torch simulator input and adding comprehensive logging to diagnose why torch outputs remained zero despite prior scale injections. Ralph made progress on instrumentation but did not complete the full canonical capture run during this iteration.

**Status**: In Progress (partial implementation)

## Key Actions

1. **Enhanced Structure Factor Logging** — Added BEFORE/AFTER scaling diagnostics to `build_structure_factor_grid()` to track amplitude transformations at line 114-120 of `scripts/generate_simple_cubic_golden.py`.

2. **Grid Statistics Instrumentation** — Expanded metadata output to include `grid_min`, `grid_max`, `grid_mean`, and detailed nonzero counts (lines 142-167), enabling verification that structure factors correctly propagate through grid construction.

3. **Raw Simulator Output Logging** — Inserted critical logging at panel simulation point (lines 495-500) to capture raw torch output device, dtype, shape, and statistics before numpy conversion, targeting the core diagnostic gap.

4. **Initiated Canonical Capture Execution** — Launched canonical dataset generation with enhanced logging via:
   ```
   CUDA_VISIBLE_DEVICES=0 KMP_DUPLICATE_LIB_OK=TRUE python scripts/generate_simple_cubic_golden.py \
     --canonical-out plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T091339Z/golden_dataset \
     --hkldebug plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T091339Z/torch_hkl_debug.json \
     |& tee plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T091339Z/canonical_capture.log
   ```

## Decisions/Rationales

- **Scale propagation via sqrt()** — Ratified existing decision (lines 115-116) that torch simulator uses squared intensity relationship: `I ∝ |F|²`, therefore scaling F by `sqrt(scale_override)` matches DiffBragg's intensity-level scaling (`docs/config_crosswalk.md:71`).

- **Dual diagnostic approach** — Added logging at two critical points: (1) structure factor amplitude layer to confirm input scaling, and (2) raw simulator output to isolate whether issue resides in grid construction, simulator kernel, or downstream conversion pipeline.

- **Device/dtype preservation** — Explicit logging of torch tensor properties (device, dtype) before conversion to numpy, per prior finding that CUDA mismatch or dtype silently zeros outputs on CPU fallback.

- **Report root coherence** — Maintained artifacts under `plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T091339Z/` to preserve ledger references documented in `docs/fix_plan.md` Attempts History (line 26).

## Errors/Failures

No fatal errors encountered in iteration 32. The canonical capture invocation executed successfully through DiffBragg refinement phase:

- **Refinement converged** — Basinhopping solver completed 5 macro cycles with residual stabilization (Iteration 7: Resid=240280.433, sigmaZ=7.109478).
- **87/92 ROIs loaded** — DiffBragg preprocessor accepted 87 reflections for refinement (5 rejected by geometry constraints).
- **No import/environment failures** — torch 2.4.1+cu121, nanobrag_torch 0.1.0, dbex all loaded cleanly without CUDA symbol errors.

**Root causes of zero-output issue (from prior context)**:
- Prior iterations (2025-10-29T091339Z planning entry) confirmed torch_max=0.0 despite flux/beamsize injection.
- Suspected root: either (1) structure factors not reaching simulator kernel, (2) GPU kernel bug in nanobrag_torch forward pass, or (3) device/dtype mismatch on output conversion.
- **This iteration's logging will isolate which stage the failure occurs** by emitting raw tensor stats at grid and simulator output boundaries.

## Evidence/Links

- **Enhanced logging commits**: `scripts/generate_simple_cubic_golden.py` lines 114-167 (scale instrumentation + grid stats) and lines 495-503 (raw simulator output logging).
- **Artifact target path**: `plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T091339Z/canonical_capture.log` (execution in progress).
- **Supporting specs**:
  - `docs/config_crosswalk.md:70-72` — Torch lacks no_Nabc_scale, scale must be pre-applied to structure factors.
  - `docs/spec-db-core.md:20-41` — Canonical tensor ordering and mask polarity requirements.
  - `docs/spec-db-conformance.md:23-26` — DB_AT_001 parity thresholds (corr ≥0.2, localization ≥0.9).
- **Related findings**:
  - `DIFFBRAGG-001` — GPU workaround for standalone diffBragg_forward (devId=0 required).
  - `PARITY-001` — Deterministic ROI metrics and artifact emission requirements.

## Next Steps

1. **Monitor canonical capture completion** — Await full execution log to inspect raw torch output statistics. If logs show nonzero simulator outputs that then zero during numpy conversion, isolate dtype/device mismatch. If simulator outputs are zero, escalate to nanobrag_torch maintainer with raw input grid stats.

2. **Inspect torch_hkl_debug.json** — Validate structure factor grid contents post-scaling via `jq` utility to confirm DiffBragg scale successfully propagated.

3. **Run panel metrics inspection** — Execute:
   ```
   jq '.[] | {panel_id, torch_max, torch_sum}' \
     plans/active/NANOBRAG-GOLDEN-001/reports/2025-10-29T091339Z/golden_dataset/torch/panel_metrics.json
   ```
   to confirm non-zero panels before fixture copy.

4. **Conditionally proceed to fixture refresh** — If torch outputs non-zero:
   - Copy regenerated tensors: `cp .../golden_dataset/torch/bragg_torch.npy tests/fixtures/golden_data/simple_cubic/bragg_torch.npy` (repeat for diffbragg, target, loss_mask).
   - Refresh manifest checksums per `docs/spec-db-conformance.md:23-26`.
   - Run parity validation: `KMP_DUPLICATE_LIB_OK=TRUE pytest -v tests/dbex/test_db_at_001_parity.py -k DB_AT_001 |& tee .../pytest_db_at_001.log`.

5. **If blocked on zero output** — Archive capture log, mark focus=blocked in `docs/fix_plan.md`, and escalate via galph_memory with raw grid stats and simulator output evidence.

## Metrics

- **Code instrumentation**: 3 logging additions (scale diagnostics, grid stats, raw simulator output).
- **Execution status**: Refinement phase completed; simulator/capture phase pending completion.
- **Tests collected**: DB_AT_001 parity (14 tests), DB_AT_001 forward equivalence (1 test) — both Active per TESTING-003.
- **Estimated impact**: Logging enhancements will provide definitive root-cause evidence for zero-output diagnosis in next iteration.
