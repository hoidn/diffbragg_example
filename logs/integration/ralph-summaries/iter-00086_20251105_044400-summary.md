# Ralph Iteration 86 Summary

**Timestamp:** 2025-11-05 03:14:07 UTC
**Initiative:** TORCH-REFINE-002 (Stage A expansion — full crystal and orientation)
**Status:** Test failure — implementation complete but 5% improvement threshold not met
**Branch:** integration

## Summary

Ralph iteration 86 implemented the Stage A expansion for LBFGS refinement, adding full crystal degrees of freedom (DoF) including unit cell lengths (a/b/c as log-deltas), angles (alpha/beta/gamma as bounded parameters), and orientation (quaternion→XYZ mapping). The implementation updated both `dbex/nanobrag_refinement.py::run_nanobrag_refinement` and the smoke test (`test_stage_a_expansion`, renamed from `test_loss_decreases`). However, the test failed because the refinement achieved only 0.15% loss improvement, falling short of the required 5% threshold.

## Key Actions

1. **Expanded Stage A parameters** in `run_nanobrag_refinement`:
   - Added `log_cell_b_delta` and `log_cell_c_delta` (complementing existing `log_cell_a_delta`)
   - Added `angle_alpha_raw`, `angle_beta_raw`, `angle_gamma_raw` (bounded angle parameters)
   - Added `orientation_vec` (3-vector mapped to unit quaternion)
   - Maintained `log_scale` warm-start and clamping (REFINE-001 guardrail)

2. **Updated LBFGS closure** to propagate all new DoFs through `crystal_overrides` dictionary, preserving tensor autograd paths per GRADIENT-001

3. **Extended telemetry** to capture deltas for all new parameters: cell b/c, angles, orientation norm

4. **Renamed and updated test**:
   - Renamed `test_loss_decreases` → `test_stage_a_expansion`
   - Updated acceptance criteria from ≥0.1% (20 steps) to ≥5% (30 steps)
   - Added verification for all new DoF deltas in `param_deltas`
   - Updated test docstring to reference TORCH-REFINE-002

5. **Test execution**: Collected and ran `pytest -v tests/dbex/test_torch_refine_smoke.py::test_stage_a_expansion --maxfail=1` with environment flags `KMP_DUPLICATE_LIB_OK=TRUE NANOBRAGG_DISABLE_COMPILE=1`

## Decisions/Rationales

- **5% improvement threshold**: Set per TORCH-REFINE-002 exit criteria for full crystal DoF refinement (vs. 0.1% for the minimal nucleus in TORCH-REFINE-001)
- **30 iteration budget**: Increased from 20 (nucleus) to accommodate additional DoF search space
- **Bounded angle parameterization**: Used tanh/clamp to prevent invalid crystal configurations
- **Quaternion orientation**: Normalized 3-vector to unit quaternion to maintain valid rotations
- **Backward compatibility**: Extended telemetry structure without replacing existing keys
- **Test rename**: Reflects expanded scope beyond minimal nucleus to full Stage A crystal parameters

## Errors/Failures and Suspected Root Causes

**Primary Failure:**
```
AssertionError: Loss improvement 0.15% < 5% threshold (TORCH-REFINE-002)
(initial=9.76e+05, final=9.75e+05)
```

**Root Cause (Suspected):**
The refinement achieved only 0.15% loss improvement despite adding full crystal DoFs. Possible causes:

1. **Well-initialized starting point**: The warm-start from geometry may already be near-optimal, limiting improvement potential
2. **Insufficient DoF movement**: Cell/angle/orientation parameters may not be moving enough to drive loss reduction (telemetry would show small deltas)
3. **ROI sampling limitations**: 15% ROI sample fraction may not provide sufficient gradient signal for full-DoF optimization
4. **Loss landscape**: The additional DoFs may be in a flat region or poorly conditioned for LBFGS
5. **Convergence tolerance**: LBFGS may be terminating early due to gradient tolerance before exploring the expanded parameter space

**Evidence from Log:**
- Test completed without runtime errors (no gradient NaN/Inf, no simulator crashes)
- Multiple HKL grid computations observed (99.73% hit rate consistent across iterations)
- Initial loss: 9.76e+05, Final loss: 9.75e+05 (improvement: 1.43e+03, 0.15%)

## Evidence/Links

**Artifacts:**
- Test log: `plans/active/TORCH-REFINE-002/reports/2025-11-05T031241Z/pytest_stage_a.log`
- Collection log: `plans/active/TORCH-REFINE-002/reports/2025-11-05T031241Z/collect_stage_a.log`

**Modified Files:**
- `dbex/nanobrag_refinement.py` — Stage A parameter expansion (cell b/c, angles, orientation)
- `tests/dbex/test_torch_refine_smoke.py` — Renamed test with updated acceptance criteria

**Key Commits (from git fast-forward):**
- Created `plans/active/TORCH-REFINE-002/implementation.md`
- Created `plans/active/TORCH-REFINE-002/reports/2025-11-05T031241Z/summary.md`
- Updates to `docs/fix_plan.md`, `galph_memory.md`, `input.md`

**Test Selector:**
```bash
KMP_DUPLICATE_LIB_OK=TRUE NANOBRAGG_DISABLE_COMPILE=1 \
pytest -v tests/dbex/test_torch_refine_smoke.py::test_stage_a_expansion --maxfail=1
```

**Test Outcome:** FAILED (1 collected, 1 failed)

## Next Steps

1. **Diagnostic Investigation:**
   - Examine telemetry `param_deltas` to quantify DoF movement (scale, cell a/b/c, angles, orientation norm)
   - Check if LBFGS terminated early due to convergence tolerance vs. iteration limit
   - Review full-loss trace to verify non-increasing behavior across validations

2. **Potential Adjustments:**
   - **Increase ROI sample fraction** (e.g., 0.15 → 0.3) to provide stronger gradient signal for expanded DoF set
   - **Relax LBFGS convergence tolerances** to allow more exploration before termination
   - **Adjust 5% threshold**: Consider whether the gate is appropriate given warm-start quality, or reduce to 1-2% for well-initialized scenarios
   - **Add per-parameter learning rates** or preconditioning to balance scale/cell/angle/orientation updates

3. **Documentation Updates:**
   - Update `docs/fix_plan.md` Attempts History with test failure details and telemetry metrics
   - Record blocked status in `plans/active/TORCH-REFINE-002/reports/2025-11-05T031241Z/blocked.md`
   - Notify in `galph_memory.md` before pausing initiative

4. **Alternative Approaches:**
   - Test with a different crystal starting point (intentionally perturbed) to verify DoFs can drive improvement when needed
   - Run full-frame optimization (ROI sample fraction=1.0) to establish upper bound on achievable improvement
   - Profile gradient magnitudes to identify which DoFs are active vs. dormant

5. **Documentation Sync (Deferred):**
   - Per input.md §Doc Sync Plan: Only update `docs/TESTING_GUIDE.md` and `docs/development/TEST_SUITE_INDEX.md` after renamed selector passes
   - Archive `pytest --collect-only` output shows 1 test collected successfully
