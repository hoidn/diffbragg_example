# Ralph Iteration 87 Summary

**Iteration:** 87
**Timestamp:** 2025-11-05T03:37:37Z
**Branch:** integration
**Session ID:** 72ec710b-98e9-4d93-b77e-b1fe705b1147
**Duration:** ~462 seconds (54 turns)
**Status:** Failed (HKL blocker encountered)

## Summary

Ralph iteration 87 focused on implementing quaternion-to-misset orientation plumbing for TORCH-REFINE-002 Stage A expansion. The core implementation succeeded in adding orientation degrees of freedom to the refinement pipeline, but testing was blocked by an HKL hit_rate=0% failure when attempting to validate with a deterministically perturbed crystal geometry. The iteration completed quaternion→XYZ Euler angle conversion and crystal config override plumbing without reintroducing previous A* injection conflicts, but could not satisfy the ≥5% improvement gate due to both the HKL mapping blocker and the inherent calibration ceiling of the refGeom dataset.

## Key Actions

1. **Quaternion-to-Misset Implementation**
   - Implemented `quaternion_to_xyz_euler()` helper function converting `orientation_vec` (3D tensor) → unit quaternion → XYZ extrinsic Euler angles (degrees)
   - Extended `create_crystal_config()` in `dbex/nanobrag_bridge.py` with `misset_deg_override` parameter
   - Wired orientation plumbing through both LBFGS closure and final Bragg generation paths
   - Applied ±3° bounding via tanh activation to keep LBFGS steps in linear regime

2. **Deterministic Perturbation Attempt**
   - Added `apply_deterministic_perturbation()` helper to test harness for perturbing refGeom crystal
   - Applied +2% cell_a stretch and ~1.5° rotation around [1,1,1] axis
   - Initial implementation used incorrect `scitbx.matrix.col.elems()` API; fixed to use numpy arrays and `get_real_space_vectors()`
   - Modified `tests/dbex/test_torch_refine_smoke.py::test_stage_a_expansion` to use perturbed crystal

3. **Test Execution**
   - Ran test with `KMP_DUPLICATE_LIB_OK=TRUE NANOBRAGG_DISABLE_COMPILE=1 pytest -v tests/dbex/test_torch_refine_smoke.py::test_stage_a_expansion --maxfail=1`
   - Test collected successfully (1 test) but failed during setup fixture phase
   - Multiple iterations to fix perturbation function implementation

## Decisions/Rationales

1. **Parameterization Strategy**
   - Quaternion parameterization chosen over direct Euler angles to avoid gimbal lock
   - XYZ extrinsic Euler convention matches nanobrag_torch expectations
   - Tanh bounding (±3°) prevents LBFGS from exploring non-linear regime

2. **No A* Injection Conflict**
   - Carefully avoided reintroducing mosflm A* injection when tensor overrides are provided (previous GRADIENT-001 finding)
   - Misset override flows through `create_crystal_config` without conflicting with cell parameter overrides

3. **Test-Only Perturbation**
   - Deterministic perturbation confined to test harness only; production defaults remain pristine (per REFINE-004 finding)
   - Rationale: refGeom dataset is too well-calibrated to satisfy ≥5% gate naturally

## Errors/Failures and Suspected Root Causes

### Error 1: AttributeError in Perturbation Function
**Location:** `tests/dbex/test_torch_refine_smoke.py:99`

```
AttributeError: type object 'col' has no attribute 'elems'
```

**Root Cause:** Incorrect usage of `scitbx.matrix.col` API. Initially attempted `col.elems(*A_original).as_mat3()` which is not the correct API pattern.

**Resolution:** Refactored to use numpy arrays and dxtbx crystal API directly:
- Used `crystal.get_real_space_vectors()` instead of A matrix manipulation
- Applied rotation matrix via numpy `@` operator
- Converted back to lists for `Crystal()` constructor

### Error 2: HKL Hit Rate = 0% (Critical Blocker)
**Context:** After fixing perturbation function, test execution proceeded but likely encountered zero HKL reflections

**Suspected Root Cause:** Perturbing crystal geometry (cell parameters and orientation) before HKL grid construction breaks Miller index mapping. When crystal is perturbed relative to the HKL grid that was constructed for the original unperturbed geometry, no Miller indices map correctly to the detector.

**Evidence:**
- Prior findings REFINE-002 and REFINE-004 document refGeom "calibration ceiling" at ~0.23% improvement
- Quote from final summary: "perturbing crystal geometry before HKL grid construction breaks Miller index mapping"

**Attempted Resolution:** Not resolved in this iteration. Requires supervisor decision on one of three options:
- (A) Source less-calibrated dataset
- (B) Accept 0.5% threshold for Stage A until Stage C detector refinement adds leverage
- (C) Build perturbation into target/background instead of geometry

## Evidence/Links

### Artifacts Directory
`plans/active/TORCH-REFINE-002/reports/2025-11-05T033936Z/`

### Key Files Modified
- `tests/dbex/test_torch_refine_smoke.py` - Added `apply_deterministic_perturbation()` helper and updated test
- `dbex/nanobrag_bridge.py` - Extended `create_crystal_config()` with misset override (implied from context)
- `dbex/nanobrag_refinement.py` - Added `quaternion_to_xyz_euler()` helper (implied from context)

### Test Logs
- `collect_stage_a.log` - Test collection showing 1 test found
- `pytest_stage_a.log` - Test execution showing AttributeError then HKL diagnostics

### Input Document
`input.md` - TORCH-REFINE-002 Do Now specifying:
- Map `orientation_vec` to bounded quaternion→XYZ misset override
- Plumb through `create_crystal_config` without A* injection
- Extend `test_stage_a_expansion` with deterministic perturbation for ≥5% gate

### Related Findings
- `docs/findings.md:33` - GRADIENT-001: Crystal parameter overrides stay as tensors
- `docs/findings.md:34` - REFINE-003: Orientation must flow through quaternion→misset
- `docs/findings.md:35` - REFINE-004: Deterministic perturbation for testing recovery
- `docs/findings.md:15` - REFINE-002: Nucleus improvement ceiling recognition

## Next Steps

1. **Supervisor Decision Required** - Choose path forward for ≥5% gate:
   - Option A: Source less-calibrated dataset with more room for improvement
   - Option B: Accept 0.5% threshold for Stage A, defer higher gate to Stage C
   - Option C: Implement perturbation in data space rather than geometry space

2. **If Proceeding with Current Approach:**
   - Investigate HKL grid construction timing relative to crystal perturbation
   - Consider perturbing geometry after HKL grid is built, or rebuilding grid with perturbed geometry
   - Document HKL mapping constraints in findings.md

3. **Alternative Validation Approach:**
   - Validate orientation plumbing works correctly (misset propagates) without requiring ≥5% improvement
   - Lower Stage A acceptance threshold to match empirical refGeom ceiling (~0.5%)
   - Defer strict ≥5% gate to Stage C when detector refinement DoFs are added

4. **Documentation Updates:**
   - Update `docs/fix_plan.md` TORCH-REFINE-002 Attempts History with 2025-11-05T033936Z entry
   - Document HKL/perturbation conflict as new finding if blocker persists
   - Update `plans/active/TORCH-REFINE-002/implementation.md` status

## Metrics

- **Turns:** 54
- **API Duration:** 340.6 seconds
- **Total Duration:** 462.7 seconds
- **Cost:** $1.57 USD
- **Model:** claude-sonnet-4-5-20250929
- **Token Usage:**
  - Input: 3,034 tokens
  - Output: 18,316 tokens
  - Cache Read: 2,878,060 tokens
  - Cache Creation: 111,279 tokens

## Final Commit

**Commit Message:** Expected "RALPH AUTO: reports evidence — tests: not run" pattern

**Files Committed:** 6 files (158,762 bytes)

**Push Status:** Pushed to integration branch (commit hash visible in git log around 9b4f670)
