# Ralph Iteration 91 Summary — 2025-11-05T06:00:58

## Summary

Ralph iteration 91 implemented TORCH-REFINE-002D, plumbing deterministic Stage A misset through nearest-neighbor HKL lookup to enable refinement smoke test validation with perturbed geometry. The implementation extracted U_delta from baseline→perturbed crystal transformation, added baseline misset tensors to orientation refinement path, disabled tricubic interpolation for Stage A per REFINE-005 policy, and updated telemetry to capture deterministic misset angles for test assertions.

## Key Actions

1. **Misset extraction and plumbing** (`dbex/nanobrag_refinement.py:172-258`):
   - Added `baseline_crystal` parameter to `run_nanobrag_refinement`
   - Extracted U_delta = U_perturbed @ U_baseline^{-1} from crystal orientation matrices
   - Converted U_delta to XYZ Euler angles using GEOMETRY-002 analytic formulas
   - Created baseline_misset_deg_tensor on refinement device/dtype

2. **LBFGS closure integration** (`dbex/nanobrag_refinement.py:381-410`):
   - Added baseline_misset_deg_tensor to orientation_vec-derived misset before `create_crystal_config`
   - Set `crystal_model.interpolate = False` to enable nearest-neighbor |F| lookup per REFINE-005

3. **Final render path** (`dbex/nanobrag_refinement.py:587-614`):
   - Mirrored misset plumbing and interpolation disabling in final full-frame render

4. **Telemetry capture** (`dbex/nanobrag_refinement.py:677-691`):
   - Captured initial baseline misset and computed total misset (baseline + optimization delta)
   - Updated param_deltas to include initial, final, delta, and quaternion_norm fields

5. **Test updates** (`tests/dbex/test_torch_refine_smoke.py:227-331`):
   - Fed `create_perturbed_geometry` output to smoke test with baseline_crystal parameter
   - Added assertions validating Z-misset ~1.5° (±0.2°) and X/Y near zero
   - Updated xfail rationale to reflect TORCH-REFINE-002D progress and remaining NN gradient limitation

6. **Validation**:
   - Ran targeted selector: 1 test collected, XFAIL in 19.98s
   - Ran full suite: 69 passed, 3 skipped, 1 xfailed, 0 failed in 639.11s (10:39)

## Decisions/Rationales

- **Nearest-neighbor over tricubic**: Per REFINE-005 policy, Stage A uses `interpolate=False` to avoid halo requirements and gradient issues with fractional HKL indices from perturbed basis
- **Misset tensor plumbing**: Added baseline_misset_deg_tensor directly to orientation_vec-derived misset (rather than reconstructing A*) per REFINE-003 to avoid conflicts with MOSFLM A* injection
- **Gradient flow preservation**: Kept misset tensors on device/dtype without `.item()`/`.detach()` per GRADIENT-001 to maintain differentiability in optimization path
- **Xfail gate deferral**: ≥5% improvement gate remains xfailed (0.22% achieved) because orientation gradient negligible with nearest-neighbor lookup; gate awaits either HKL grid rebuild (Option A) or larger perturbation amplitude (Option B)
- **Telemetry structure**: Captured both initial (baseline) and final (total) misset to enable test assertions validating deterministic perturbation propagation

## Errors/Failures and Suspected Root Causes

No errors or failures occurred in this iteration. All tests passed or xfailed as expected:

- **Targeted test xfail**: `test_stage_a_expansion` triggered xfail condition (improvement <5%) as expected due to negligible orientation gradient with nearest-neighbor HKL lookup, not a code defect
- **Full suite clean**: 69 passed, 3 skipped (pre-existing), 1 xfailed (expected), 0 failed — no regressions introduced

## Evidence/Links

- **Implementation artifacts**: `plans/active/TORCH-REFINE-002D/reports/2025-11-05T070500Z/`
  - `collect_stage_a.log` — Test collection (1 test)
  - `pytest_stage_a.log` — Targeted test run (XFAIL in 19.98s)
  - `pytest_full_suite.log` — Full suite results (69/3/1/0)
  - `summary.md` — Detailed implementation summary with ADR alignment and search results

- **Code changes**:
  - `dbex/nanobrag_refinement.py:172-258,381-410,587-614,677-691` — Misset extraction, plumbing, and telemetry
  - `tests/dbex/test_torch_refine_smoke.py:227-331` — Perturbation integration and assertions

- **Ledger updates**:
  - `docs/fix_plan.md:73` — Added 2025-11-05T070500Z attempt with metrics and next actions

- **Metrics**:
  - HKL hit rate: 98.07% (6103634/6224001) with fractional indices
  - Deterministic misset telemetry: initial=[0.0, 0.0, 1.5] degrees (Z-component matches +1.5° perturbation)
  - Improvement: 0.22% (below ≥5% gate per nearest-neighbor gradient limitation)

- **Git commit**: `750cfd9` "TORCH-REFINE-002D: Plumb deterministic Stage A misset through nearest-neighbor HKL"

## Next Steps

1. **Exit criteria assessment**: Mark TORCH-REFINE-002D exit criteria 1+3 satisfied (telemetry plumbing complete, ≥95% hit rate achieved with 98.07%, Attempts History updated); criterion 2 (≥5% gate) remains blocked by negligible orientation gradient with nearest-neighbor lookup

2. **Follow-on options**:
   - **Option A (HKL grid rebuild)**: Spawn initiative to reindex structure factors against perturbed A*, enabling larger perturbation or restoring tricubic interpolation with halo (per 2025-11-05T041539Z analysis)
   - **Option B (larger perturbation)**: Explore increased amplitude (e.g., +3-5% cell, +3-5° rotation) to assess orientation gradient headroom without grid rebuild

3. **Documentation closure**: If closing TORCH-REFINE-002D, update `docs/spec-db-conformance.md` DB-AT-025 to note Stage A `interpolate=False` policy and REFINE-005 mitigation status

4. **Gradient magnitude measurement** (optional per `input.md:45`): Profile gradient magnitudes after misset plumbing to size HKL grid rebuild or perturbation amplitude adjustments
