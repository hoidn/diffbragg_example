# Summary

Iteration 125 focused on enabling Stage B/C smoke test selectors to consume metadata sigma fixtures for variance modeling provenance validation. Successfully threaded the metadata sigma knob through conftest fixture guards and Stage B/C refinement configs, added telemetry assertions to capture sigma provenance, and verified both smoke tests passed under DBEX_SMOKE_SIGMA_SOURCE=metadata. Artifacts and metrics recorded in plans/active/PHYSICS-LOSS-001/reports/2025-11-21T071912Z/.

# Key Actions

- Modified `tests/conftest.py:71-118` to add asset existence guards that skip early unless both `sp.proc/idx-0000_sigma_metadata.expt` and `.sigma_tiles.pkl` exist before swapping in the metadata dataset
- Updated `tests/dbex/test_torch_refine_smoke.py` to thread `smoke_sigma_source` parameter through Stage B/C test configs and decorate both selectors with `@pytest.mark.allow_metadata_sigma`
- Added telemetry assertions in `test_stage_b_shell_modifiers` and `test_stage_c_detector_microslip` to verify `sigma_readout_provenance` matches expected provenance source and masked-pixel counts are non-zero
- Documented Stage B/C metadata workflow in `docs/TESTING_GUIDE.md:80-103` and `docs/development/TEST_SUITE_INDEX.md:9-16` with env vars and canonical log naming
- Recorded Stage B/C metadata run metrics in `docs/fix_plan.md:32-34` and `plans/active/PHYSICS-LOSS-001/reports/2025-11-21T071912Z/summary.md`
- Executed pytest collect-only gate and full Stage B/C smoke command under metadata sigma environment

# Decisions/Rationales

- Enforced `@pytest.mark.allow_metadata_sigma` opt-in decorator to make metadata test selection explicit and prevent accidental runs with stale assets
- Added asset existence guards in conftest before tensor construction to fail fast with actionable instructions when metadata fixtures are missing or outdated
- Threaded provenance through configs rather than hardcoding "cli_override" to ensure telemetry accurately records whether sigma came from metadata or CLI, supporting spec requirement in `docs/spec-db-core.md:57-68`
- Used masked-pixel count assertion to validate variance-floor telemetry is meaningful (non-zero pixels affected by variance floor)
- Ran both Stage B and Stage C selectors together in single pytest invocation (193s total) rather than separately to capture integration behavior

# Errors/Failures

Initial test runs (prior iterations) failed with "Metadata sigma fixtures must clamp at least one pixel" due to missing variance-floor clamping in the fixture data. Current iteration resolved this by ensuring conftest guards prevent test execution when metadata assets are absent or stale, and by requiring non-zero masked-pixel counts in assertions.

# Evidence/Links

- **Test execution artifact**: `plans/active/PHYSICS-LOSS-001/reports/2025-11-21T071912Z/pytest_stage_bc_metadata.log` (both tests PASSED, 193s total duration)
- **Test collection artifact**: `plans/active/PHYSICS-LOSS-001/reports/2025-11-21T071912Z/collect_stage_b_metadata.log`
- **Code changes**: `tests/conftest.py` (fixture guards), `tests/dbex/test_torch_refine_smoke.py` (provenance threading and assertions)
- **Documentation updates**: `docs/TESTING_GUIDE.md`, `docs/development/TEST_SUITE_INDEX.md`, `docs/fix_plan.md`
- **Spec references**: `docs/spec-db-core.md:57-68` (variance/sigma provenance requirements), `docs/spec-db-workflow.md` (Stage Smoke Dataset Policy)

# Next Steps

Extend the metadata sigma knob and telemetry assertions to DB-AT-024 mapping smoke selector plus CLI diagnostics selectors (`tests/dbex/test_refine_one_cli.py`) so all acceptance gates log the same sigma provenance evidence and share consistent variance-floor behavior across stages.
