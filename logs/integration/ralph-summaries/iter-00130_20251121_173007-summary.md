# Summary

Ralph implemented per-panel `nanobrag_torch.Simulator` caching within StageAContext to eliminate warm-path re-instantiation overhead. The Stage A smoke test passed, and warm vs cold benchmarking shows a measured speedup of approximately 1.01× (warm=162.78 s vs cold=163.80 s), confirming that simulator reuse alone does not yet reach the 2–5× performance target. Changes were committed to integration branch (8bb4676..2493107).

# Key Actions

- Extended `StageAContext` to cache per-panel `nanobrag_torch.Simulator` instances alongside detector/HKL/mask tensors
- Added helper functions to hydrate and retarget cached `Crystal` models with correct beam config and HKL state per closure
- Rewired `compute_loss` so warm mode never instantiates simulators inside the panel loop while preserving the cold rebuild path for benchmarking
- Executed `tests/dbex/test_torch_refine_smoke.py::test_stage_a_expansion` with CLI sigma overrides and small detector fixture (PASSED)
- Reran benchmark script `plans/active/PERF-WARM-SIM-001/bin/benchmark_stage_a_cache.py --modes warm cold` and captured telemetry/perf counters
- Updated `docs/fix_plan.md` with new attempt entry (2025-11-21T100053Z) and metrics summary
- Added new finding PERF-WARM-004 to `docs/findings.md` documenting the 1.01× plateau
- Committed 10 files (181289 bytes) to integration branch with auto-report artifacts

# Decisions/Rationales

- **Simulator pooling**: Pre-building simulators inside StageAContext avoids per-closure re-instantiation, aligning with `docs/spec-db-runtime.md` warm-cache reuse requirements
- **Beam config sharing**: Both warm and cold paths now derive and use `BeamConfig` for consistency, ensuring parity in physics calculations
- **Cold path preservation**: The cold branch remains untouched to maintain a valid performance baseline for future comparisons
- **Telemetry logging**: Closure counts, forward times, and final χ² values logged to `telemetry_stage_a_small.json` and `benchmark_summary.json` for reproducibility

# Errors/Failures

None reported. Test execution completed successfully (test_stage_a_expansion PASSED). Benchmark executed without errors and produced valid metrics. No environment violations or missing imports encountered.

# Evidence/Links

- Test artifact: `tests/dbex/test_torch_refine_smoke.py::test_stage_a_expansion` (PASSED)
- Implementation scope: `dbex/nanobrag_refinement.py:293-1010`
- Benchmark metrics: `plans/active/PERF-WARM-SIM-001/reports/2025-11-21T093905Z/benchmark_summary.json`
- Telemetry: `plans/active/PERF-WARM-SIM-001/reports/2025-11-21T093905Z/telemetry_stage_a_small.json`
- Performance counters: `plans/active/PERF-WARM-SIM-001/reports/2025-11-21T093905Z/{warm,cold}_perf_counters.json`
- Documentation updates: `docs/fix_plan.md:37-55`, `docs/findings.md:12-15`
- Git commit: `8bb4676..2493107` (integration branch)

# Next Steps

1. Profile `Simulator.run()` hotspots (panel loop allocation patterns) and ROI/loss-mask materialization to identify the next amortization target
2. Once bottlenecks are confirmed via profiling, prototype caching for ROI sampling and loss-mask tensor prep to approach the ≥2× speedup goal
3. Only extend the simulator pool pattern to Stage B/C closures after Stage A demonstrates >1.3× improvement
4. Keep warm/cold telemetry aligned during any new caching phases to maintain benchmark validity
