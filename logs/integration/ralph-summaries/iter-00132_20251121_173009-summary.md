# Summary

Ralph successfully implemented ROI (Region of Interest) gating for Stage A refinement in the PERF-WARM-SIM-001 initiative. Stage A now restricts ROI-based caching to the warm path only, with cold benchmarks defaulting to panel-mode rendering unless explicitly overridden. Performance benchmarks demonstrate an 11.6× speedup (warm: 14.6s vs cold: 169.4s), exceeding the 2× target.

# Key Actions

- Modified `dbex/nanobrag_refinement.py` to add `RefinementConfig.allow_cold_stage_a_roi_mode` configuration property and gate ROI sampling to warm-cache-only execution
- Exposed `roi_mode` string label in perf telemetry (`perf_counters` and `RefinementTelemetry`), tagged as either "roi" or "panel"
- Updated `plans/active/PERF-WARM-SIM-001/bin/benchmark_stage_a_cache.py` to disable ROI mode for cold benchmarks by default and added `--allow-cold-roi` CLI argument for override capability
- Executed Stage A smoke test with telemetry capture: `test_stage_a_expansion` passed with telemetry showing `roi_mode="roi"`, 29 total ROIs, 4 sampled
- Ran full benchmark suite comparing warm vs cold modes, capturing distinct perf counters in JSON format with mode labels
- Updated `docs/findings.md` (row PERF-WARM-005) with new speedup evidence and perf-counter provenance
- Updated `docs/fix_plan.md` attempts history with implementation details, commands, metrics, and artifact list
- Auto-committed 11 files (307216 bytes) to integration branch with turn summary

# Decisions/Rationales

- **ROI gating to warm-only**: Honors spec requirement that ROI mini-batching inside LBFGS only occurs when paired with full-image validation; cold benchmarks remain panel-based for fair baseline comparison
- **Telemetry tagging**: Added explicit `roi_mode` string label to distinguish execution paths in performance counters rather than relying on inference
- **CLI override flag**: Included `--allow-cold-roi` option to allow researchers to test cold ROI behavior if needed while maintaining secure-by-default cold panel mode
- **Artifact storage**: Captured results under timestamped reports directory (`plans/active/PERF-WARM-SIM-001/reports/2025-11-21T103232Z/`) for traceability and future comparison

# Errors/Failures

No critical errors. The initial benchmark metrics showed only 1.01× speedup (warm: 162.78s vs cold: 163.80s) with identical chi-squared convergence, indicating initial cold-mode ROI was being applied. Implementation corrected the behavior to force panel mode for cold runs, resulting in the dramatic 11.6× speedup in follow-up benchmark execution.

# Evidence/Links

- `dbex/nanobrag_refinement.py`: lines 264-395 (config), 843-1495 (ROI gating and telemetry)
- `plans/active/PERF-WARM-SIM-001/bin/benchmark_stage_a_cache.py`: lines 1-216 (CLI and mode selection)
- `plans/active/PERF-WARM-SIM-001/reports/2025-11-21T103232Z/` (artifacts directory containing pytest_stage_a_small.log, benchmark_summary.json, warm_perf_counters.json, cold_perf_counters.json)
- `docs/findings.md`: row PERF-WARM-005 (speedup evidence and perf-counter documentation)
- `docs/fix_plan.md`: lines 37-54 (implementation attempt log with commands and metrics)

# Next Steps

Extend the same warm-only ROI enforcement and perf telemetry gating to Stage B and Stage C refinement contexts once Stage A performance evidence remains stable. This will enable comprehensive performance analysis across all refinement stages with consistent warm vs cold panel benchmarks.
