# Summary

Ralph iteration 00134 focused on hardening Stage B/C perf telemetry for the warm-cache refinement path on the canonical full detector. The iteration implemented perf-counter assertions in smoke tests, extended telemetry metadata to track cache and ROI modes, replayed the canonical Stage B/C tests, and re-ran the warm-vs-cold benchmark to document reproducible performance gains. All tests passed and performance benchmarks confirmed an 11.45x speedup (warm 14.77 s vs cold 169.12 s).

# Key Actions

- Implemented Stage B/C perf-counter assertions in `tests/dbex/test_torch_refine_smoke.py` to enforce `cache_mode="warm"`, `roi_mode="panel"` (Stage B), and `roi_mode="roi"` (Stage C) with validation of ROI counts, closure evaluations, and forward-time metrics.
- Extended `dbex/nanobrag_refinement.py` to derive ROI totals from `refinement_inputs.panel_slices` and record sampled ROI counts per stage, piping cache/ROI metadata into perf counters and `RefinementTelemetry`.
- Executed smoke tests with full-detector configuration (`DBEX_SMOKE_DETECTOR_SIZE=full`) and telemetry output path: `pytest -vv tests/dbex/test_torch_refine_smoke.py::{test_stage_b_shell_modifiers,test_stage_c_detector_microslip}` (2 tests passed).
- Re-ran warm-vs-cold benchmark via `benchmark_stage_a_cache.py --modes warm cold` to capture canonical full-detector performance deltas.
- Updated `docs/findings.md` to register new lesson PERF-WARM-007 documenting the perf-counter contract and benchmark evidence.
- Logged all artifacts (logs, telemetry JSON, benchmark summaries, perf counters) to `plans/active/PERF-WARM-SIM-001/reports/2025-11-21T133500Z/`.

# Decisions/Rationales

- **Perf assertions as gates**: Stage B/C tests now fail if perf counters drift (cache_mode mismatches, ROI count regressions) so warm-cache defects are caught immediately rather than silently degrading performance.
- **ROI totals from panel slices**: Canonical ROI count (92 slices) is derived from `refinement_inputs.panel_slices` to ensure test assertions stay synchronized with actual detector geometry.
- **Full-detector as canonical baseline**: Running with `DBEX_SMOKE_DETECTOR_SIZE=full` provides the authoritative performance reference for warm vs cold paths on the actual detector configuration.
- **Telemetry JSON archival**: Perf counter metadata now flows into `_record_stage_telemetry` payloads so telemetry JSON is a single source of truth for performance and caching state.

# Errors/Failures

No errors or test failures encountered. All assertions passed and benchmark completed successfully.

# Evidence/Links

- Test file: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/tests/dbex/test_torch_refine_smoke.py` (lines 970-1004 for Stage B asserts; lines 780-834 for Stage C asserts)
- Refinement implementation: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/dbex/nanobrag_refinement.py` (lines 1946-2008, 2518)
- Findings ledger: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/docs/findings.md` (PERF-WARM-007 entry)
- Test artifacts: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/plans/active/PERF-WARM-SIM-001/reports/2025-11-21T133500Z/` (pytest_stage_bc_full.log, telemetry_stage_bc_full.json, benchmark_summary.json, warm_perf_counters.json, cold_perf_counters.json)

# Next Steps

Continue PERF-WARM-SIM-001 backlog by implementing Stage B ROI-aware sampling and cache reuse so that shell modifiers can operate on subsets of ROIs without regressing the newly established perf telemetry gates. This will enable per-ROI caching strategies and further performance optimization opportunities.
