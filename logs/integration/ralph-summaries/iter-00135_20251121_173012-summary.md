# Summary

Ralph iteration 135 implemented Stage B ROI-mode execution that reuses warmed ROI simulators from Stage A, enabling shell modifiers to operate on cached ROI entries instead of whole panels. Small-detector smoke tests passed with ROI mode enabled and accurate telemetry counters. Full-detector smoke test failed due to test fixture config mismatch: both detector sizes activate ROI mode when panel slices exist, but the test expects panel mode fallback for canonical full-detector runs.

# Key Actions

- Implemented `compute_loss_stage_b` ROI-mode branch (dbex/nanobrag_refinement.py:1702-1740) that iterates `stage_a_ctx.roi_entries` when `use_stage_b_roi_mode` is active instead of simulating full panels.
- Added ROI sampler to Stage B: samples ~15% of ROI work items when ROI mode active, mirrors Stage A sampling strategy (lines 1601-1613).
- Updated perf counters to track active mode: `roi_mode`, `roi_count_total`, `roi_count_sampled` now reflect ROI vs panel execution (lines 2000-2014).
- Updated test assertion: `tests/dbex/test_torch_refine_smoke.py::test_stage_b_shell_modifiers` (line 977) now expects `roi_mode` to follow `config.enable_stage_a_roi_mode` + panel_slices presence.
- Executed small-detector smoke test with telemetry capture: PASSED with roi_mode="roi", roi_count_total=29, roi_count_sampled=4, Stage B improvement 23.7% vs Stage A.
- Executed full-detector smoke test: FAILED with assertion error "Stage B roi_mode should be 'panel', got roi" (line 978).
- Committed implementation with detailed rationale noting test fixture config issue.

# Decisions/Rationales

- ROI-mode branching: Stage B mirrors Stage A's ROI knob to eliminate redundant whole-panel re-simulation; cold branch remains unchanged for benchmarking.
- Perf counter alignment: Following Stage C precedent, Stage B surface active mode (roi vs panel) through telemetry so regressions are immediately visible.
- Test fixture behavior: Both detector sizes have panel_slices>0 and `enable_stage_a_roi_mode=True` by default, causing both to activate ROI mode. The original test intention was to vary roi_mode by detector size, but the implementation varies it by config flag.

# Errors/Failures

- Full-detector smoke assertion failure (tests/dbex/test_torch_refine_smoke.py:978): expected `roi_mode="panel"` but got `roi_mode="roi"`. Root cause: test fixture doesn't differentiate config by detector size; both small and full detectors see ROI mode when ROI entries exist and `enable_stage_a_roi_mode=True`.

# Evidence/Links

- Code changes: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/dbex/nanobrag_refinement.py` (lines 1595-1740: ROI mode branch and sampling)
- Test updates: `/home/ollie/Documents/diffbragg_example_2/diffbragg_example/tests/dbex/test_torch_refine_smoke.py` (line 977: roi_mode assertion)
- Small-detector smoke log: `plans/active/PERF-WARM-SIM-001/reports/2025-11-21T121804Z/pytest_stage_b_small.log` (PASSED)
- Small-detector telemetry: `plans/active/PERF-WARM-SIM-001/reports/2025-11-21T121804Z/telemetry_stage_b_small.json`
- Full-detector smoke log: `plans/active/PERF-WARM-SIM-001/reports/2025-11-21T121804Z/pytest_stage_b_full.log` (FAILED, assertion error at line 978)
- Commit: `2150172` "PERF-WARM-SIM-001: Stage B ROI mode mirrors Stage A ROI sampling (tests: small-detector smoke passed)"
- Fix plan update: `docs/fix_plan.md` (PERF-WARM-SIM-001 attempts history, line 57)

# Next Steps

1. Resolve test fixture config mismatch: Either set `enable_stage_a_roi_mode=False` for full-detector canonical runs via test parametrization, or realign test expectations to accept ROI mode for all runs with `len(panel_slices)>0`.
2. Re-run full-detector smoke test once config is adjusted to verify both small and full detector runs pass with telemetry capturing ROI/panel counts.
3. If both smokes pass, extend canonical-detector validation per input.md: assert perf_counters contract and run `benchmark_stage_a_cache.py --modes warm cold` to refresh warm-vs-cold metrics with new ROI counters.
4. Update findings ledger with PERF-WARM-XXX entries once full-detector gate passes and ROI totals stabilize.
