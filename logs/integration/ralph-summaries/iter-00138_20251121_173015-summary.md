# Summary

Ralph's iteration 138 attempted to implement the PERF-WARM-009 fix by adding a `force_panel_eval` flag to Stage B validations, ensuring initial/periodic/final loss checks evaluate full detector panels (reusing warmed simulators) rather than ROI-sampled loss. Despite correctly implementing panel-scoped evaluations, the strict ±1% REFINE-008 shell-modifier gate still failed with shell_0_modifier=2.0, indicating the root cause is not ROI-only evaluations but genuine chi-squared improvement at that modifier value.

# Key Actions

- Added `force_panel_eval` parameter to `compute_loss_stage_b()` in dbex/nanobrag_refinement.py (lines 1615-1807)
- Updated three validation calls (initial, periodic, final) to pass `force_panel_eval=True` (lines 1866, 1843, 1892)
- Panel evaluations now iterate full detector via `list(range(n_panels))` while ROI closures continue for perf counters
- Ran Stage B smoke tests on both small (passed) and full (failed) detector configurations
- Generated telemetry logs and captured pytest output with detailed failure signatures

# Decisions/Rationales

- Chose to force panel evaluation only during validation checks, not optimization closures, to preserve ROI-mode perf counters per PERF-WARM-005/008
- Reused Stage A's warmed simulators via `stage_a_ctx` to avoid re-instantiation overhead
- Kept ROI perf counters (`roi_mode="roi"`, sampled counts) unchanged to maintain findings accuracy
- Documented the blocker in commit message rather than cascading failure downstream

# Errors/Failures

**Primary Blocker:** Full-detector smoke test failed the strict ±1% shell-modifier gate:
- Test assertion: `shell_0_modifier (d=80000.12-100000.00Å) drifted by 1.0000 (>±1%)`
- Expected range: ±0.01, Actual: 2.0 (multiplicative modifier at maximum clamp)
- Small-detector smoke passed with same modifier value (2.0), suggesting detector size or ROI sampling affects chi-squared landscape

**Root Cause Hypothesis:** Panel evaluations are correctly implemented and in use (verified via code inspection and telemetry chi-squared traces showing 708M→540M improvement, 23.7% gain). The fact that shell_0_modifier=2.0 genuinely improves panel-scoped chi-squared contradicts PERF-WARM-009's assumption that "ROI-only evaluations caused the drift." This suggests the root cause lies deeper: either (a) Stage B's structure-factor or interpolation logic drifts at high d-spacing (shell_0), (b) the variance-weighting in chi-squared calculation favors shell_0=2.0, or (c) REFINE-008's ±1% gate is too strict for canonical detectors.

# Evidence/Links

**Implementation:**
- /home/ollie/Documents/diffbragg_example_2/diffbragg_example/dbex/nanobrag_refinement.py (lines 1615-1807, 1866, 1843, 1892)

**Test Results:**
- plans/active/PERF-WARM-SIM-001/reports/2025-11-21T142700Z/pytest_stage_b_small.log (passed)
- plans/active/PERF-WARM-SIM-001/reports/2025-11-21T142700Z/pytest_stage_b_full.log (failed assertion at line 1060)
- plans/active/PERF-WARM-SIM-001/reports/2025-11-21T142700Z/telemetry_stage_b_small.json (shell_0=2.0, roi_mode="roi", cache_mode="warm")

**Git Commit:**
- Commit: 44c6c7f "PERF-WARM-SIM-001: Add panel-evaluation path for Stage B validations (blocked)"

**Documentation:**
- docs/fix_plan.md (Attempts History updated with blocker note and metrics)
- plans/active/PERF-WARM-SIM-001/reports/2025-11-21T142700Z/summary.md (turn summary)

# Next Steps

1. **Escalate blocker to supervisor:** Panel evaluations alone do not prevent shell_0_modifier=2.0; deeper Stage B investigation required
2. **Review Stage B math:** Verify tricubic interpolation, structure-factor calculation, and variance-weighting don't inherently favor shell_0=2.0 at high d-spacing
3. **Profile chi-squared landscape:** Run Stage B with shell modifiers constrained to [0.99, 1.01] to see if chi-squared improves or worsens (diagnostic)
4. **Re-examine REFINE-008 intent:** Clarify if ±1% gate applies to canonical detectors or only to perturbed geometries; consider if the gate should be relaxed for this detector configuration
5. **Do not proceed with ROI summary script:** Blocked pending upstream clarification on whether panel evaluations are the correct fix or a red herring

