# Summary

Ralph iteration 140 (2025-11-21 13:45:14) executed the telemetry-ordering fix for Stage B smoke tests within the PERF-WARM-SIM-001 initiative. The work wrapped the Stage B smoke test body in a try/finally block to ensure telemetry is always written before the strict REFINE-008 gate fires, enabling canonical-detector runs to produce telemetry even on failure. Small-detector smoke passed with 23.7% chi-squared improvement; full-detector smoke encountered CUDA out-of-memory errors during Stage B closure optimization, preventing the gate from being evaluated on canonical data.

# Key Actions

- Wrapped `tests/dbex/test_torch_refine_smoke.py::test_stage_b_shell_modifiers` test body (lines 969–1134) in try/finally so `_record_stage_telemetry()` always executes before strict REFINE-008 asserts
- Ran small-detector smoke test: passed with `roi_mode="roi"`, improvement 23.7%, shell_0_modifier clamped at 2.0, telemetry successfully recorded
- Ran full-detector smoke test: pytest passed but Stage B internal error (CUDA OOM) prevented closure optimization from completing; telemetry captured the failure state
- Executed summarize script (`plans/active/PERF-WARM-SIM-001/bin/summarize_stage_b_roi.py`) to consolidate both datasets into `stage_b_roi_summary.json`
- Confirmed telemetry JSON files (`telemetry_stage_b_small.json`, `telemetry_stage_b_full.json`) written to artifacts directory despite test failure

# Decisions/Rationales

- **Telemetry before gate**: Ordering change ensures Stage B chi-squared traces are captured for post-mortem analysis even when shell-modifier tolerance or CUDA constraints prevent test passage. This breaks the feedback loop where failed tests left no telemetry.
- **Strict gate left intact**: REFINE-008 asserts remain in place and still fail on canonical runs, but now with full diagnostic data available. Decision preserves safety and coverage while unblocking telemetry capture.
- **Full-detector OOM treated as Stage B error**: Rather than masking the CUDA exhaustion, telemetry records it as `status="error"`, `roi_count_sampled=92` (all panels), improvement 0%, all modifiers at identity. This documents the true blocker.

# Errors/Failures

- **CUDA out of memory (6.0 GiB)**: Full-detector smoke triggered GPU memory exhaustion during Stage B LBFGS closure. System had 21.58 GiB in use; allocation request exceeded available 6.0 GiB. Root cause likely warm-cache simulator holding full 21.6M-pixel detector state in VRAM; small-detector run (29 panels, ~1.8M pixels) succeeded within same cache footprint.
- **Canonical gate still not passed**: Due to OOM, full-detector refinement never completed enough closure iterations to exercise the ±1% shell-modifier tolerance gate. Cannot verify whether panel-scope evaluations (PERF-WARM-009) prevent shell_0=2.0 clamp.

# Evidence/Links

- Test implementation: `tests/dbex/test_torch_refine_smoke.py:969–1134` (try/finally wrapper)
- Telemetry artifacts: `plans/active/PERF-WARM-SIM-001/reports/2025-11-21T150000Z/{telemetry_stage_b_small.json,telemetry_stage_b_full.json,stage_b_roi_summary.json,pytest_stage_b_small.log,pytest_stage_b_full.log}`
- Plan context: `docs/fix_plan.md:149–150` (PERF-WARM-SIM-001 planning and implementation entries)
- Summarize script: `plans/active/PERF-WARM-SIM-001/bin/summarize_stage_b_roi.py`

# Next Steps

1. **Investigate Stage B CUDA memory profile**: Profile memory usage during warm-cache closure (peak VRAM, panel-count scaling) to determine if detector size is the bottleneck or if closure history/gradients accumulate beyond expected footprint.
2. **Consider CPU-fallback or batch chunking for canonical runs**: If full detector cannot fit in VRAM, evaluate switching canonical (`smoke-detector-size=full`) runs to CPU or implementing sub-panel batching in the closure loop to reduce peak memory.
3. **Defer PERF-WARM-009/010 validation**: Shell-modifier gate behavior cannot be assessed until full-detector Stage B completes at least one full refinement cycle. Once OOM is resolved, re-run and inspect `telemetry_stage_b_full.json` for panel-evaluation traces.
4. **Review `enable_stage_a_roi_mode=False` threading**: 2025-11-21T133127Z planning noted that Stage A ROI sampling may be causing canonical drift. Confirm whether Stage A should be forced to panel-scope for canonical runs to stabilize the shell-modifier signature.
