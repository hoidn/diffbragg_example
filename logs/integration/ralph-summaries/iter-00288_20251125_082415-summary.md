# Ralph Iteration 288 Summary

**Timestamp**: 2025-11-25T005459Z
**Status**: Handoff → Galph
**Commit**: 8821c9c9c47a50aa368b619d546542fa800820b3
**Sync Commit**: 7493c0a (ralph → next=galph status=ok)

## Summary

Ralph iteration 288 focused on **Phase D.C calibration schema alignment and persistent telemetry plumbing** for the TOOLING-VIS-001 diagnostics initiative. Ralph completed schema restructuring for smoke calibration loading, ensured calibration_path tracking across all diagnostic surfaces (Stage A tests DB-AT-028/029 and mapping probes), and verified that the canonical DiffBragg `config_torch.json` schema now loads correctly.

Key finding: Calibration metadata is now loading correctly (spot_scale_override correctly moves from 1.0 to 3.105e+17), but downstream physics remains pathological (negative ROI correlations, chi²/pixel violations). The schema fix and calibration plumbing are complete; a separate modeling/geometry gap remains.

## Key Actions

1. **Calibration Schema Restructured**: Reshaped `capture_smoke_calibration.py` to emit nested `crystal.{scale_override, N_cells}` and `beam.{flux, exposure, beamsize_mm}` instead of flat JSON structure, aligning with `dbex.nanobrag_bridge.load_calibration_metadata` expectations.

2. **Calibration Path Plumbing**: Extended mapping diagnostics (`dbex/vis/mapping.py`), Stage A smoke tests (DB-AT-028/029 in `tests/dbex/test_stage_a_smoke_parity.py`), and CPU/GPU comparison probe (`compare_mapping_forward_cpu_gpu.py`) to extract and persist `calibration_path` in all telemetry JSONs.

3. **DBEX_SMOKE_CALIB_PATH Env Integration**: Updated mapping context builder to honor `DBEX_SMOKE_CALIB_PATH` environment variable (fallback to `sp.proc/calibration/config_torch_smoke.json`), enabling clean calibration config path override in test/diagnostic workflows.

4. **Test Coverage**: Executed DB-AT-028 (loss-scale sanity) and DB-AT-029 (structure/intensity parity) selectors with calibration metadata resolved; both tests FAILED as expected (known physics gap), but artifacts confirm calibration_path is correctly captured and spot_scale_override loads from config.

## Decisions/Rationales

- **Nested schema adoption**: Matching canonical DiffBragg config structure (crystal/beam nesting) rather than custom flat JSON ensures compatibility with existing `load_calibration_metadata` loader and simplifies future upstreaming.
- **Environment variable plumbing**: Using `DBEX_SMOKE_CALIB_PATH` allows flexible calibration override without code changes, supporting multiple fixture scenarios (metadata vs external calibration).
- **Persistent telemetry**: Capturing calibration_path in every diagnostic surface (mapping, Stage A, CPU/GPU probe) meets hard gate requirements (CONFORMANCE-001) and enables audit trails.

## Errors/Failures

**DB-AT-028**: chi²_per_pixel_initial=1.084e+05 (spec ≤1e2, ~1000x violation), final=1.08e+05
- Suspected root cause: Geometry zero-point mismatch or HKL grid misalignment despite correct calibration loading

**DB-AT-029**: median_corr_before=−0.054 (spec ≥0.2), negative correlation
- Suspected root cause: Model/data structure mismatch; calibration loads but physics still diverges

**Both failures are expected** and documented in Phase D.C exit criteria. Calibration schema fix and plumbing are complete; next phase (D.D zero-point probe) targets the remaining physics gap.

## Evidence/Links

- Commit: `8821c9c` (RALPH AUTO: reports evidence — tests: not run)
- Sync commit: `7493c0a` ([SYNC i=288] actor=ralph → next=galph status=ok)
- Reports directory: `plans/active/TOOLING-VIS-001/reports/2025-11-25T005459Z/`
  - `db_at_028/db_at_028_metrics.json`: chi²_per_pixel, spot_scale_override=3.105e+17, calibration_path resolved
  - `db_at_029/db_at_029_metrics.json`: correlation metrics, spot_scale_override=3.105e+17, calibration_path resolved
  - `pytest_db_at_028_029.log`: 2 tests FAILED (expected, known physics gap)
  - `pytest_db_at_028_029_collect.log`: 2 tests collected
  - `manual_stage_a_metrics.txt`: Diagnostic summary showing variance_floor_masked_pixels=168,912, chi2/pixel ratios, telemetry early_stop, baseline telemetry records spot_scale_override, log_scale_baseline
  - `mapping_metrics.json` (db_at_024): Global baseline (chi²_per_pixel=75.64, corr_median=0.621, mapping config archival)

**Files modified in iteration 288**:
- `dbex/nanobrag_refinement.py`: +411 lines (log_scale clamping, log_cell delta clamping, telemetry variance_floor pixel tracking, calibration_baseline parameter propagation)
- `dbex/refinement/stage.py`: +6 lines (calibration propagation)
- `dbex/refinement/stage_a.py`: +32 lines (variance_floor telemetry accumulation)
- `tests/conftest.py`: +17 lines (fixture updates)
- `tests/dbex/test_stage_a_smoke_parity.py`: +289 lines (DB-AT-028/029 selectors, calibration_path extraction, metrics JSON emission)
- Test artifacts (metrics JSONs, test logs, diagnostics) archived per CONFORMANCE-001

## Next Steps

1. **Phase D.D zero-point probe**: Implement explicit Stage A zero-parameter forward evaluation (all param_deltas[*]['final'] = ['initial']) to isolate mapping alignment vs calibration construction gap
2. **Audit calibration derivation**: Verify spot_scale_override derivation from metadata fixture (sqrt(spot_scale_override) → log_scale_baseline), flux/exposure products, N_cells unit conversions
3. **Geometry baseline validation**: Confirm U(0) and B(0) in Stage A match mapping baseline (spec-db-workflow.md:45–46)
4. **Rerun DB-AT-028/029**: Once zero-point calibration gap resolved, verify chi²/pixel and ROI correlation recovery against spec tolerances
