### Turn Summary
Scoped Phase C.2 torch writer extraction, updating docs/fix_plan.md and input.md so Ralph has a ready-to-run Do Now for moving `_write_torch_outputs` into `dbex/io/writer.py`.
Reaffirmed DIAGNOSTICS-001/PHYSICS-LOSS telemetry constraints and mapped the two CLI pytest selectors plus env knobs needed to validate the shared writer.
Next: implement the shared writer + CLI rewiring and run the mapped CLI tests, capturing logs in the new report directory.
Artifacts: plans/active/ARCH-REFINE-001/reports/2025-12-01T132921Z/
