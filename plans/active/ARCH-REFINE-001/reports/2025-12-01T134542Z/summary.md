### Turn Summary
Outlined Phase C.3 so the gradcheck-only forward/loss helpers move into `dbex.physics`, updated docs/fix_plan.md plus the implementation checklist, and carved out the new module targets under ARCH-REFINE-001.
Rewrote input.md with the precise Do Now (physics module extraction, bridge re-exports, DB-AT-010 selector plan) so Ralph can implement immediately.
Next: execute the helper migration, update the DB-AT-010 tests/docs, and rerun the gradcheck selector under the new artifacts directory.
Artifacts: plans/active/ARCH-REFINE-001/reports/2025-12-01T134542Z/
