### Turn Summary
Queued per-panel Stage B parity diagnostics plus canonical snapshot capture so REFINE-FLOW-001 guard failures emit actionable JSON instead of the current stub.
Refreshed docs/fix_plan.md and input.md with the new Do Now, mapped tests, and pitfalls so Ralph can implement the instrumentation + targeted unit test immediately.
Next: Ralph codes the guard updates, writes the deterministic guard unit test, and reruns the Stage B/C small smokes plus the new unit selector.
Artifacts: plans/active/ARCH-REFINE-001/reports/2025-12-01T153327Z/ (input.md, planning notes)
