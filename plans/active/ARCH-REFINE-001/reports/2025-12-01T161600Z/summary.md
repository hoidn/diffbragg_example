### Turn Summary
Scoped the Stage B parity helper/test plan so the REFINE-FLOW-001 guard can be validated without spinning up fake optimizers.
Recorded the Phase E.3 Do Now (helper extraction + CPU fallback test rewrite + Stage B/C smokes) and refreshed docs/fix_plan.md + input.md with commands, env vars, and pitfalls for Ralph.
Next: Extract the guard helper, update the baseline diff test to hit both pass/fail paths, then rerun the Stage B/C small-detector smokes with telemetry under this report directory.
Artifacts: plans/active/ARCH-REFINE-001/reports/2025-12-01T161600Z/ (input.md, docs/fix_plan.md diffs)
