### Turn Summary
Mapped the simulator-intensity probe evidence to a trusted-mask gap and logged the C.6 plan so reconstruction uses the same masked detector configs as Stage A.
Refreshed the implementation plan, fix-plan log, galph memory, and Do Now to capture the cold-path mask change plus the required probe + DB-AT validation steps.
Next: implement the trusted-mask plumbing, rerun the comparison script, and re-execute DB-AT-028/029 with the new artifact dirs.
Artifacts: plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-03T161601Z/
