| Tap Point | Path:Line | Rationale | Expected Units / Notes |
|-----------|-----------|-----------|------------------------|
| `panel_mask_candidate` device + dtype before and after the reconstruction guard | `dbex/refinement/reconstruction.py:205-225` | Confirms whether the trusted mask that enters the cold path actually reaches the simulator on the CUDA device; currently the tensor stays on CPU and therefore never participates in the detector mask. | Log `type(panel_trusted_mask)`, `.device`, and `.float().mean()` for each panel; coverage should match Stage A context (0.90 on refGeom_small panel 0). |
| `normalized_mask` emitted by `create_unified_simulator` | `dbex/refinement/helpers.py:108-135` | Shows whether mask normalization ever runs for reconstruction (it does for Stage A warm caches because they skip the factory). If `normalized_mask is None` while `inputs.trusted_mask` is non-null, we know the mask was dropped. | Capture `normalized_mask is None` and, when present, its `.device` to ensure CUDA runs keep the data on GPU. |
| Stage A vs reconstruction raw means / ratio | `plans/active/ARCH-SIM-CONSTRUCTION-001/bin/compare_simulator_outputs.py` (Stage A + reconstruction paths) | Maintains a regression log proving whether the 0.8468 StageA/Reconstruction ratio collapses to 1.0 after mask normalization changes. | Extend the JSON output with the per-panel mask stats and `normalized_mask_in_use` flag so future loops can verify parity without rerunning pytest. |
