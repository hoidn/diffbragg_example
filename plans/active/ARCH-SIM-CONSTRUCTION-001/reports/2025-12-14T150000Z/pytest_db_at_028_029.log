Summary: DB-AT-028 still failing with chi²/pixel=2.097e5 after wiring StageAArtifacts.

Warm cache is now working (stage_a_artifacts available, ratio 0.988 in baseline probe).
However, the reconstruction masked mean (0.39 ADU) remains 30× below telemetry (11.57 ADU).

Root cause hypothesis: The reconstruction helper's masked-mean computation uses inputs.loss_mask,
but the telemetry's model_mean_masked was computed during LBFGS using a different mask
(possibly ROI-sampled or full-panel validation mask). The unmasked mean matches (14.4 ADU),
confirming the simulator output is correct but mask alignment is off.

Next loop should investigate mask provenance in Stage A telemetry emission
(check if model_mean_masked comes from ROI vs full-panel validation).
