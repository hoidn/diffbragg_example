### Turn Summary

Aligned Stage A baseline probe HKL/calibration inputs with DB-AT-028/029 smoke fixture logic per input.md Do Now. Updated `compare_stage_a_baseline.py::get_refgeom_dataload()` to mirror `tests/conftest.py:210-251` detector-size-aware resolution: honours `DBEX_SMOKE_CALIB_PATH`/`DBEX_SMOKE_HKL_PATH` overrides, defaults to small-detector refined MTZ when calibration exists, falls back to raw `scaled.mtz` with `I(+),SIGI(+),I(-),SIGI(-)` columns otherwise. Added resolved `mtz_file`, `mtz_col`, and `calibration_config_path` to `probe_metadata` block and console logging. Validated probe run with small-detector smoke env vars; output JSON now captures aligned HKL paths. Ran DB-AT-028/029 selector; artifacts written to `plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-15T010000Z/db_at_{028,029}/`. Tests still fail with same parity signatures (chi²/pixel=2.1e5, median_corr_before=-0.054), confirming the input alignment does not resolve the underlying simulator construction divergence. Next step: analyze the parity evidence (bragg_before vs telemetry model_mean_masked 9.3× ratio) to localize the first divergence in the reconstruction path.

Artifacts: `plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-15T010000Z/` (`stage_a_baseline_probe.json`, `pytest_db_at_028_029.log`, `db_at_028/db_at_028_metrics.json`, `db_at_029/db_at_029_metrics.json`)
