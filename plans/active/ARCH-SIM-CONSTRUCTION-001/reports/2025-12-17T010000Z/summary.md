### Turn Summary

Stage A vs mapping “parity failures” from the 2025-12-16 probe were an instrumentation artifact: the probe always perturbs the crystal/detector/beam with the Stage A smoke offsets, so comparing that perturbed state against the DB-AT-024 mapping baseline will always produce huge chi²/ROI deltas. Rather than escalate to a spec change, this loop schedules a Parity-mode Do Now for Ralph to add a `--geometry-mode {perturbed,baseline}` flag to `compare_stage_a_baseline.py`, record the selected mode in the JSON output, rerun the probe in baseline mode, and then refresh the DB-AT-028/029 evidence bundle under `plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-17T010000Z/`. This keeps ARCH-SIM-CONSTRUCTION-001 scoped to simulator construction while giving us a clean zero-delta parity check before the next implementation attempt.
