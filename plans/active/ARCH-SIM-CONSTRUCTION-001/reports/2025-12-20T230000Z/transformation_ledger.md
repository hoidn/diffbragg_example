# Transformation Ledger — Stage A vs Independent Reference

Boundary: zero-iteration Stage A simulator outputs (`build_final_bragg_from_stage_a_telemetry`) compared against the DIALS reflection-table intensities and HKL amplitudes captured by the Stage A baseline probe (`plans/active/ARCH-SIM-CONSTRUCTION-001/bin/compare_stage_a_baseline.py:611-807`). Evidence sampled from `plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2025-12-20T210000Z/stage_a_baseline_probe_baseline.json`.

| Field | Units / Frame / Order | Producer (file:line) | Consumer (file:line) | Observed Evidence | Hypothesis |
| --- | --- | --- | --- | --- | --- |
| Masked mean baseline (`target_mean_masked`, `model_mean_masked`, `chi²/pixel`) | ADU/pixel, detector loss mask ordering | `dbex/refinement/stage_a.py:403-520` (telemetry export of masked means + chi² inputs) | `tests/dbex/test_stage_a_smoke_parity.py:335-515` (DB-AT-028/029 chi² + ROI-correlation gates) | Probe JSON reports `target_mean_masked=87.118 ADU`, `model_mean_masked=87.118 ADU`, yet `chi_squared_per_pixel_initial=9.80e5` because per-ROI energy distribution diverges (`reconstructed_bragg_before.mean_masked`, `mapping_comparison`). | Global baseline scaling matches, so deterministic failure is spatial redistribution before DB-AT gates. Need to follow intensity flow into per-ROI HKL contributions instead of adjusting scalar baselines. |
| ROI 0 Panel 0 `[897:909,17:29]`, HKL (-10,2,0) | ADU/pixel in detector frame (ROI mask) | `plans/active/ARCH-SIM-CONSTRUCTION-001/bin/compare_stage_a_baseline.py:611-747` (ROI ↔ reflection matching) | `tests/dbex/test_stage_a_smoke_parity.py:398-515` (median ROI-corr floor) | `stagea_mean=0.0199` vs `target_mean=7.81`; reflection mean `8.69`; `amp_sq_per_pixel=9.95`; Stage A under-signals by **≈500×** (`stagea_vs_ref_ratio=2.3e-3`, `stagea_vs_amp_sq_ratio=0.0020`). | Simulator drops almost all energy for this Miller index even though reference + HKL amplitude agree, implying lookup or normalization error inside nanobrag_torch’s scattering kernel. |
| ROI 1 Panel 0 `[644:656,21:33]`, HKL (-7,5,-3) | ADU/pixel (ROI mask) | same as above | same as above | `stagea_mean=5.8e-4` vs `target=7.67`; `amp_sq_per_pixel=4.34`; Stage A produces **7.1e-5×** of the reference signal. | Multiple adjacent HKLs collapse to ~0 intensity, so the defect is systematic (not isolated reflection noise). |
| ROI 2 Panel 0 `[257:269,37:49]`, HKL (-3,8,-9) | ADU/pixel | same | same | Strong reflection (`target=82.84`, reference `83.10`) still yields `stagea_mean=0.244` with `stagea_vs_amp_sq_ratio=4.8e-4`. | Even bright peaks lose ~99.95% of energy, pointing toward incorrect HKL sampling or normalization across the entire simulator, not ROI masking. |
| ROI 14 Panel 0 `[431:443,434:446]`, HKL (0,2,-2) | ADU/pixel | same | same | Outlier where Stage A **overestimates**: `stagea_mean=407.7` vs `target=2.48`; `stagea_vs_ref_ratio=234`, `stagea_vs_amp_sq_ratio=10.4`. | Some HKLs explode while most collapse, indicating coordinate-axis mismatch or double-counting rather than a single missing scalar. Need HKL-query telemetry to see which lattice region is sampled. |
| ROI 21 Panel 0 `[787:799,694:706]`, HKL (-2,-6,5) | ADU/pixel | same | same | Median-like case: `stagea_mean=11.80`, `target=192.2`, `amp_sq_per_pixel=65.94`; Stage A is 0.06× the reference and 0.18× `|F|^2`. | Confirms ~10× deficit for the bulk of reflections while respecting the target distribution shape. The simulator outputs appear to be mapped to the wrong HKL frame (energy conserved but shifted), motivating HKL-query stats on the same dataset. |

**Conclusion:** Stage A + mapping conserve total masked energy but redistribute it to the wrong HKL buckets. References (reflection table + HKL amplitudes) agree, so the deterministic crisis originates inside the nanobrag_torch sampling path. Next boundary bisection: capture HKL query ranges (min/max h,k,l and hit rates) for this exact dataset to prove whether the simulator is evaluating indices outside the populated grid.
