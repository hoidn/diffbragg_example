# ARCH-SIM-CONSTRUCTION-001 — Phase C.39 planning (2026-01-11T180000Z)

## Loop Summary
- Re-reviewed the latest instrumentation bundle (`reports/2026-01-11T010000Z/`) which shows `trace_subpixel_F_total_sq_sum=1.6148e17` and `trace_normalized_intensity=1.6148e11` (normalized/raw = 1e-6) whenever oversample>1 for SQUARE lattices, while oversample=1 remains perfect. This isolates the remaining `(Na·Nb·Nc)^2` deficit to the omega application site inside `Simulator.run`.
- Updated `plans/active/ARCH-SIM-CONSTRUCTION-001/implementation.md` and `docs/fix_plan.md` with Phase C.39 scope: drop the per-subpixel omega multiply for SQUARE/oversample>1, apply omega once after the sum, preserve instrumentation, and tie the work directly to Problems ledger item “DB-AT-028/029 scale mismatch (SCALE-009…)”.
- Authored a patch-ready Do Now in `input.md` instructing Ralph to edit `src/nanobrag-torch/src/nanobrag_torch/simulator.py::Simulator.run`, refresh `tests/architecture/test_nanobrag_partiality.py::test_square_lattice_applies_ncells`, keep `probe_square_lattice_scaling.py` telemetry aligned, capture the vendor diff (`patches/omega_compensation.patch` + env tag), update `docs/findings.md::SIM-CONSTR-PARTIALITY-001`, and rerun the probe + enforcement test + DB-AT-028/029 under this timestamp.
- Mapped validation commands explicitly: (1) sanctioned single-pixel probe (oversample=13), (2) `pytest -vv tests/architecture/test_nanobrag_partiality.py::test_square_lattice_applies_ncells --maxfail=1`, (3) `pytest -vv tests/dbex/test_stage_a_smoke_parity.py -k "DB_AT_028 or DB_AT_029"` with `DBAT028_ARTIFACT_DIR` / `DBAT029_ARTIFACT_DIR` pointing at this report tree.
- Findings enforced: SIM-CONSTR-PARTIALITY-001 (stay inside the simulator), PROBE-FREEZE-001 (no new plan-local probes), SCALE-009 (DB-AT gate). DecisionStatus=patch_ready ⇒ Forbidden: no new probes, no plan-bin edits, no Stage A/mapping tweaks.

## Next Steps for Ralph
1. Modify `Simulator.run` oversample>1 SQUARE branch so `last_omega` is not applied before the Riemann sum; multiply by the omega scalar exactly once afterward, keep partiality stats intact, and capture the diff as `patches/omega_compensation.patch` with a new env tag logged in `patches/environment_tag.md` + `docs/findings.md`.
2. Update `tests/architecture/test_nanobrag_partiality.py::test_square_lattice_applies_ncells` to enforce ≤1% tolerance on cpu/cuda legs post-fix (including telemetry checks), plus adjust `probe_square_lattice_scaling.py` so the Markdown/JSON highlight the restored normalized/raw ratio.
3. Run the mapped probe + pytest commands listed above (authoritative env vars set) and stash logs + DB-AT metric JSON files under `plans/active/ARCH-SIM-CONSTRUCTION-001/reports/2026-01-11T180000Z/` before reporting back.
