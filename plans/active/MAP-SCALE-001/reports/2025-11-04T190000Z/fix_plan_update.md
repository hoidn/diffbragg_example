  * 2025-11-04T190000Z (implementation) — Implemented beam flux/beamsize/exposure and crystal N_cells plumbing per input.md Do Now: Extended `load_calibration_metadata()` (dbex/nanobrag_bridge.py:606-695) to parse beam.beamsize_mm and crystal.N_cells from config_torch.json; updated `create_beam_config()` (392-442) and `create_crystal_config()` (445-509) to accept optional calibration overrides with kwargs pattern to avoid passing None (fixes TypeError in BeamConfig.__post_init__); refactored `simulate_forward_once()` (822-942) to accept calibration dict and route values into config builders; updated test (tests/dbex/test_mapping_consistency.py:183-212,251-258) to pass calibration dict. Test ran with flux=1e12, beamsize_mm=1.0, exposure=1.0, N_cells=[36,28,26] but produced severe regression: corr_median=-0.0431 (was 0.0190), bragg_mean=2.97e10 ADU (was 1.32e6), 3.8e8× larger than canonical bragg_torch masked mean (77.5 ADU). Root cause: BeamConfig computes fluence=flux·exposure/beamsize²=1e18 photons/m², which gets multiplied into simulator output; then sqrt(spot_scale_override)=5.643e8 multiplies again, double-counting the intensity scaling since spot_scale_override already includes the flux effect from DiffBragg refinement. Hypothesis: config_torch.json beam parameters are diagnostic metadata (documenting what the canonical generator used) not runtime parameters for zero-iteration simulation; correct approach is to run simulator WITHOUT flux/beamsize/exposure (using defaults) and apply only sqrt(spot_scale_override) post-sim per SCALE-002. Implementation technically correct (code does what input.md requested) but test results indicate the Do Now was based on misinterpretation of config_torch.json semantics. Metrics: n_roi=92, corr_median=-0.0431, localization=7.61%, bragg_stats.mean=2.97e10, calibration fields populated (beamsize_mm=1.0, N_cells=[36,28,26]). Artifacts: plans/active/MAP-SCALE-001/reports/2025-11-04T190000Z/{summary.md,pytest_db_at_024.log,mapping_metrics.json,mapping_metrics.csv}. First Divergence: Passing flux/beamsize/exposure to BeamConfig inflates intensities by ~22,500× compared to prior run (without calibration), and by ~3.8e8× compared to canonical. Alternative interpretation: if flux/beamsize/N_cells ARE required for physics alignment, then spot_scale_override should NOT be applied when calibration dict is provided (contradicts SCALE-002). Next Actions: **BLOCKED** pending supervisor clarification on whether config_torch.json beam parameters are (1) diagnostic-only or (2) runtime calibration with different scaling logic. Recommendation: Revert flux/beamsize/exposure plumbing from simulate_forward_once; keep only N_cells if it affects mosaic physics independently. Full pytest suite passed (66 passed, 3 skipped, 0 failed) after fixing kwargs pattern to avoid passing None to dataclass constructors.
