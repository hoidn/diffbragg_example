| Tap name | Path:line | Rationale | Expected units/order |
| --- | --- | --- | --- |
| stage_b_shell_modifier_raw | dbex/nanobrag_refinement.py:1286-1358 | Log the per-shell modifiers right before and after cloning `hkl_grid` so we can confirm LBFGS updates anything beyond the identity seed and catch NaN/Inf gradients before the guard trips. | Tensor of length `stage_b_n_shells`; values are multiplicative factors (unitless) near 1.0. |
| stage_b_chi_squared_best | dbex/nanobrag_refinement.py:1494-1500 | Instrumenting `chi_squared_best_b` and `best_params_snapshot_b` lets us observe whether the closure ever records a better validation point; currently `nonlocal` is missing so the tap will prove the variable never mutates. | Scalar chi-squared (photons^2) plus iteration index. |
| stage_c_detector_offset_override | dbex/nanobrag_bridge.py:322-338 & 420-444 | Sampling the `distance_mm_override` passed into `create_detector_config` ensures Stage C actually offsets detector panels before simulation and gives us the true initial ±0.25 mm baseline needed for reduction metrics. | Distance in millimeters; sign alternates with panel id. |
| stage_c_param_deltas_serialized | dbex/nanobrag_refinement.py:2017-2034 | Capture the dict before it’s returned so we can cross-check against injected offsets; adding actual baseline fields prevents the smoke from assuming zero initial offsets. | JSON-ready map keyed by `panel_{id}_distance_offset_mm`, entries should include baseline, final, delta (mm). |
| simulator_hkl_neighborhood | src/nanobrag-torch/src/nanobrag_torch/models/crystal.py:210-427 | Logging the `(B,4,4,4)` HKL subcube allocation size per Stage B call will highlight why the canonical detector OOMs and whether ROI downsampling or CPU fallbacks are triggered. | h×k×l voxels (unitless index space) with magnitude matching HKL grid dimensions. |
| telemetry_chi_squared_trace | tests/dbex/test_torch_refine_smoke.py:34-55 | Emit the last two Stage A and Stage C chi-squared samples to artifacts so parity reviewers can see the flat traces without opening HDF5 telemetry. | Ordered list `[iteration, chi_squared]`; same photons^2 units as RefinementTelemetry. |
