# Manual CLI Test (Legacy Backend) - DEFERRED TO USER TESTING

## Command Skeleton

python -m dbex.refine_one --backend diffbragg \
  -e <expt.json> -r <refl.refl> -i 0 \
  -o /tmp/tooling_vis_test_legacy.h5 \
  --report-dir /tmp/tooling_vis_triptychs_legacy \
  -m <mask.pickle> -z <mtz.mtz>

## Expected Behavior

1. HDF5 file created at /tmp/tooling_vis_test_legacy.h5 with variance datasets (Phase B.1)
2. Directory /tmp/tooling_vis_triptychs_legacy/ created if it doesn't exist
3. PNG files generated: roi_0000_triptych.png, roi_0001_triptych.png, etc.
4. CLI prints: "Triptych report saved to: /tmp/tooling_vis_triptychs_legacy"

## Validation Status

DEFERRED - Compilation check is PRIMARY validation gate per input.md note.
Manual validation requires golden_data with full refinement run (>5 minutes).
User should test with real data to confirm end-to-end behavior.
