# Torch Backend Manual Test Command (Deferred - Golden Data Unavailable)

## Command Skeleton
python -m dbex.refine_one --backend nanobrag \
  -e golden_data/simple_cubic/expt_000000.json \
  -r golden_data/simple_cubic/indexed_000000.refl \
  -i 0 \
  -o /tmp/tooling_vis_test_torch.h5 \
  --report-dir /tmp/tooling_vis_triptychs_torch \
  -m golden_data/simple_cubic/mask.pickle \
  -z golden_data/simple_cubic/mtz.mtz

## Status
DEFERRED - requires user data. Compilation check is PRIMARY validation gate per input.md.

## Expected Behavior
1. Refinement completes successfully
2. Directory /tmp/tooling_vis_triptychs_torch/ is created
3. PNG files roi_0000_triptych.png, roi_0001_triptych.png, etc. are generated
4. CLI prints "Triptych report saved to: /tmp/tooling_vis_triptychs_torch"

## Backward Compatibility Note
Running without --report-dir flag should work unchanged (feature is opt-in).
