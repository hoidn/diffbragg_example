### Turn Summary
Scoped the next TOOLING-VIS-001 loop around adding masked/unmasked scale diagnostics to `emit_mapping_context_diagnostics` so metadata-smoke mapping artifacts record the calibration clues we lack.
Authored a ready-for-implementation Do Now directing Ralph to extend the helper, run the metadata_scaled vs metadata_refined probe, and rerun DB-AT-028/029 under the metadata env so the refreshed JSON/logs prove whether calibration or HKL provenance drives the ROI anti-correlation.
Next: implement the diagnostics change, capture the probe + pytest artifacts, then decide if the fix should correct MTZ selection or calibration handling based on the new evidence.
Artifacts: plans/active/TOOLING-VIS-001/reports/2025-11-25T130000Z/ (input.md, mapping_dataset_metrics/)
