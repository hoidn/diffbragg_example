
## 2025-11-22T235959Z — TORCH-GEOMETRY-CONVERGENCE-001 Phase C2 LR Reduction Fix Handoff
- Focus: TORCH-GEOMETRY-CONVERGENCE-001 — Diagnose & Fix Quaternion U-Matrix Catastrophic Convergence Failure
- Action Type: ready_for_implementation
- Key Observations: Reviewed Ralph's Phase C1 convergence telemetry (commit 7424a8f, artifacts 2025-11-22T230000Z/) confirming decisive root cause identification. Telemetry analysis (10-step A_scale_only convergence test with --use-u-matrix) shows: (1) Initialization HEALTHY (chi²_before=1.13M per Phase B5 B_ideal fix, zero-point parity perfect correlation=0.9999999843), (2) First optimizer step CATASTROPHIC OVERSHOOT (chi²_step0=8.84M, +679% jump from init), (3) Subsequent steps SLOW MONOTONIC RECOVERY (chi² decreasing -0.009%/step, final=8.83M), (4) Gradients HEALTHY (log_scale~150k stable, no NaN/Inf, monotonic decrease), (5) Quaternion norm PERFECT (q_norm=1.0 exact all steps). **Root cause verdict:** H1 (Adam LR too high) with HIGH confidence (~85%). Mechanism: LR=1e-4 designed for cell/misset gradients O(1-100) but quaternion gradients are O(150k) → Adam update Δq = -1e-4 × 150k = -15.0 (massive ~35° rotation) causes geometry to jump to bad local minimum. Physics interpretation documented in phase_c1_decision.md §Physics Interpretation (line 87-109). H2 (variance instability) UNLIKELY (no oscillations), H3a (NaN/Inf gradients) REJECTED (all flags false), H3b (gradient explosion) SYMPTOM ONLY (gradients large but proportional to chi², not pathological), H4 (quaternion constraint) NOT TESTABLE (A_scale_only has train_orientation=False). **Recommended fix:** Reduce Adam LR to 1e-5 for U-matrix path (10× reduction from cell/misset LR=1e-4) via new `RefinementConfig.u_matrix_learning_rate` field. Authored comprehensive Phase C2+C3 Do Now directing Ralph to: (1) Extend RefinementConfig with u_matrix_learning_rate field (default 1e-5), (2) Update run_nanobrag_refinement Stage A optimizer setup with conditional LR selection (lr = config.u_matrix_learning_rate if config.use_u_matrix_parameterization else 1e-4), (3) Extend stage_a_mapping_adam_debug.py with --u-matrix-lr CLI flag, (4) Phase C2 validation test (A_scale_only with LR=1e-5, expect chi² drift ≤5% and CC≥0.99), (5) Phase C3 validation test (D_full with LR=1e-5, CONDITIONAL on C2 success, expect monotonic improvement), (6) Decision synthesis with 3-path template (Path A SUCCESS: proceed to C4-C6; Path B PARTIAL: tune LR further; Path C FAIL: escalate to LBFGS/Riemannian Adam), (7) Regression guard test_stage_a_expansion, (8) Update implementation.md checklist, (9) Emit summary. Implementation floor satisfied: Do Now contains production code tasks (RefinementConfig field, optimizer conditional LR, script CLI flag) AND validating pytest selector (test_stage_a_expansion regression guard). New artifacts directory: plans/active/TORCH-GEOMETRY-CONVERGENCE-001/reports/2025-11-22T235959Z/. Decision tree: If C2 succeeds (chi²≤1.2M, CC≥0.99) → proceed to C4 (extended regression guard), C5 (draft CONVERGENCE-002 finding documenting LR incompatibility root cause + fix), C6 (close initiative, unblock PARITY-003/PARITY-002/REFINE-002E); if C2 partial (chi²<8.8M but >1.2M) → try LR=1e-6 or gradient clipping; if C2 fails (chi²>8M) → escalate to LBFGS or alternative parameterization.
- Artifact Path: plans/active/TORCH-GEOMETRY-CONVERGENCE-001/reports/2025-11-22T235959Z/
- Next Actions: Ralph implements Phase C2 LR reduction fix (config field + optimizer conditional + CLI flag), executes Phase C2 validation test (A_scale_only with --u-matrix-lr 1e-5), conditionally runs Phase C3 (D_full if C2 succeeds), synthesizes decision.json per 3-path template, runs regression guard, updates implementation.md checklist, and emits summary. Next loop (if Path A) proceeds to Phase C4-C6 (extended regression guard, CONVERGENCE-002 finding, initiative close).
- <Action State>: [ready_for_implementation]

2025-11-22T235959Z focus=TORCH-GEOMETRY-CONVERGENCE-001 state=ready_for_implementation dwell=0 artifacts=plans/active/TORCH-GEOMETRY-CONVERGENCE-001/reports/2025-11-22T235959Z/ next_action=phase_c2_lr_reduction_fix_implementation_and_validation
